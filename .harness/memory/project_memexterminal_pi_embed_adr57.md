---
name: project_memexterminal_pi_embed_adr57
description: "MemexTerminal X 梁定案 = Pi SDK 库内嵌(R-A)+图为工作记忆(C3);ADR-57 落定,tracer 待切"
metadata: 
  node_type: memory
  type: project
  originSessionId: f0d02284-1fa9-46a0-ac80-ba178d6eb08a
---

2026-06-15 fuller 会话收口。MemexTerminal 到 Hermes/Pi-like 真 agent TUI 的设计定案,反转了 shipped 的「`--agent` retired / 一个应答者」决定(现降为 **per-surface 律**:channel=对话核心无工具,terminal=Pi agentic 全工具)。

**两个核心决策(fuller 逐分支拍板):**
- **R-A 库内嵌**(否决 R-B 原生协议远程=重造 MCP):MemexTerminal = 基于 `@earendil-works/pi-coding-agent` 的进程,in-process import MemexCore 函数,**不走 MCP**。用 Pi 是为 DRY——loop/审批/TUI 自带。
- **provider 接线纠正(D-2 改写,180966d1)**:**config-share 非 in-process 委托**。Core `LLMProvider`=`chat→string` 非流式非 OpenAI 协议,委托会让 Pi 丧失原生 tool-calling;正解=把 Core onboarded 配置(baseUrl/key/model/api)喂 pi ModelRegistry,Pi 直连同一 endpoint(实测 nvidia qwen3.5,OpenAI 兼容)。脑/key 同一个靠共享配置。机制=`createAgentSessionServices({modelRegistry})`+`createAgentSessionFromServices({services,model,customTools,noTools:'builtin'})`,models.json schema=`{providers:{<name>:ProviderConfig}}`。**provider 接线 = DONE 活体绿(2ec4861f)**:落 `packages/terminal-pi`(resolveCoreProvider 用 loadMemexConfig+${ENV}插值 / buildSessionWithCoreBrain);真 nvidia qwen3.5 一轮回 `MEMEX-PI-OK`,spike harness + in-repo tsx 各一次。**bug 修复**:fresh worktree 缺 .env(gitignore 不复制未跟踪)→ 缺 NVIDIA_API_KEY,拷主 .env 进 worktree 即修。nvidia profile 本就 api='openai-completions'。worktree 已全量 npm install(加 pi-coding-agent,**勿 npm audit fix --force**)。**已整合回 master**(ff-merge 9a809006→后续 a20ed5c3,worktree 已 remove,均推送 origin/master)。代码在 `packages/terminal-pi`(provider-bridge.ts + run-nvidia/run-c3/run-ledger.mts)。

**build-out line #2 (C3 注入实体) = 机制证明绿(9a809006,真 LLM)**:in-process `ExtensionFactory` 经 `createAgentSessionServices({resourceLoaderOptions:{extensionFactories}})` 注入(这是之前的未解结);`before_agent_start`→systemPrompt 注哨兵投影,nvidia 回哨兵=**投影到达脑**;`agent_end` 抓整轮 messages[]=回写料够;`run-c3.mts`。**写回半边=真账本绿(a20ed5c3)**:`run-ledger.mts` 真 pg pool + `nestScope`(@graph/control-plane/nesting,子路径导入非 root)建真 scope+分区→nvidia 一轮→`agent_end`→真 `occWrite`(memory_updated/conversation.assistant)→查 execution_event_log 确认落账(occ_result=won,hash 吻合)。**C3 闭环=全绿(73f9d1ee,真 Core 函数多轮)**:读回 `before_agent_start`→真 `processAgentTurn`(写 user turn+内部真 assembleContext)+真 `loadConversationHistory`→注入 systemPrompt;**gateway 新增子路径导出 `./conversation`+`./process-agent-turn`**(原只 `.`,深导入被封)。`run-c3-loop.mts` 多轮+**每轮 fresh pi session(零保留)**:turn1 告知 teal→写图;turn2 全新 session 经图重投影回 "Teal"=**Graph=工作记忆证明**。注:reply 必须从 agent_end messages 捕获(message_update 时序空);agent_end async flush 加 settle 轮询;nestScope 用同 pool 当 ddlPool。**残留(非阻塞)**:systemPrompt 调优(用 CONVERSATION_SYSTEM_ROLE 非 pi 默认)/embed 现传 null/接进 MemexTerminal 真入口(现独立 run 脚本)。**line #3 审批=绿(814f87c0)**:`tool_call` hook 拦截 gated `shell_exec`→`checkCommand` 判 block→`ApprovalService.request`(双写#1 行+requested 审计)→决策(headless 走 CommandGate 策略,TUI 走 `ctx.ui.confirm`)→`decide`(双写#2 denied 审计)→`{block:true}`。`run-approval.mts` 验:executed=false + approval_request.status=denied + 审计 requested/denied 双落库。gateway 加导出 `./security/approval`;typebox 加 terminal-pi 依赖。**ADR-57 D-1~D-5 全部实证完成**(R-A嵌入/C3图记忆双向/provider config-share/审批双写+block);**残留 D-6 两新工具 send_message+schedule_task 未建**;非阻塞残留=systemPrompt 调优(CONVERSATION_SYSTEM_ROLE)/embed投影/ctx.ui.confirm 真TUI活体/接进 MemexTerminal 真入口(现5独立脚本)。**terminal-pi 5 run 脚本全绿**:run-nvidia/run-c3/run-ledger/run-c3-loop/run-approval。gateway 导出: ./process-agent-turn ./conversation ./security/approval。
- **C3 图为工作记忆**(否决 C1 账本=审计后盾):每 turn 从图投影注入 Pi(`before_agent_start`),turn 末冲回账本(`turn_end`);Pi 的 message list 从不是权威状态。保「Graph→Context」在 terminal 也成立,点亮 assembleContext/reflection/CCR/晶化全套。详见 [[project_now_realtime_and_hermes_gap]] [[project_memex_terminal_design]]。

**Pi 能力 ctx7 全验绿** (`/earendil-works/pi`):registerProvider(streamSimple)/registerTool/on(before_agent_start|turn_end|tool_call)+ctx.ui.confirm。脑=Core onboarded provider 经 streamSimple 同进程委托(免key)。审批=tool_call hook 双写 ApprovalService。

**包谱系 npm 实测**:`@earendil-works/pi` **404 不存在**(docs 的 `pi` 是 coding-agent 层对象非包名);嵌入目标 = `@earendil-works/pi-coding-agent` v0.79.3,desc 逐字 "read, bash, edit, write tools and session management"(用户三目标自带),`main`+`exports` 有程序化入口。辅:pi-agent-core(更底层 loop)/pi-ai(provider)/pi-tui(渲染)。

**落点**:ADR-57 = `docs/adr/0066-adr57-memexterminal-pi-embed.md`;分支 `feat/memexterminal-pi-embed`(worktree `D:/Repo/graph-enginerring-pi-embed`);commits e5ba6385(ADR+notes)+efa1817c(tracer 绿),**均未推**。隔离装跑在仓库外 `D:/Repo/pi-embed-spike`(一次性)。GH #25 epic / ROADMAP §23 架构基石。

**tracer 状态 = 实跑绿(efa1817c)**。真 API 已读 `.d.ts` 核实:嵌入入口 `createAgentSession({noTools,customTools,sessionManager})→{session}`;驱动 `session.prompt/subscribe`;工具 `defineTool`+`customTools[]`;hook `ExtensionAPI.on(...)` in-process 走 `loadExtensionFromFactory`。**kill-criterion 双绿(类型+活体)**:`before_agent_start`="Fired after user submits prompt but before agent loop"=per-user-prompt(C3 注入点,且它是 extension-runner 事件**不在 subscribe 流**,注入必须走 extension handler);冲回点据活体改为 `agent_end`(per-prompt 带整轮 messages[],比 turn_end 干净);`tracer.mjs` 实跑序列 agent_start→turn_start→message×2→turn_end→agent_end。`createAgentSession` 无 key 也跑完一轮(默认兜底,provider 非阻塞)。

**build-out 残留 seam(非阻塞)**:①provider 接线=Core `LLMProvider`→pi-ai `Model`/registerProvider(pi-ai 包布局异常整仓发布,走 coding-agent 转出的 ModelRegistry/getModel,勿深挖)②安全:`noTools:'builtin'` 后 `getToolDefinition('bash')` 仍非空(疑 enabled≠registered),build-out 按实际 enabled 集核验 pi 裸 bash 真关、只暴露 Core 容器化 execute_bash ③C3 实体:before_agent_start handler 调 `assembleContext`+history→`{messages,systemPrompt}`,agent_end handler `occWrite` 冲回。
