{
  "version": "1.0",
  "phase": "20-autonomous-assistant",
  "task": null,
  "status": "arc-complete",
  "last_updated": "2026-06-13T06:05:00.000Z",
  "current_state": "Channel + security hardening arc (2026-06-13). Started from a /goal (channel deep-dive of hermes — DRY + UX), then a /fuller session that established a live-verification policy and drove it into concrete fixes. Core insight reused all session: 'logic-done + unit tests green' ≠ wired/working — three instances surfaced (Telegram undici last session, execute_bash docker, Slack connector), all only caught by LIVE verification. Policy adopted: the 'done' line sits at the ENVIRONMENT BOUNDARY (network/OS/runtime/external-peer/deploy = live-mandatory; pure logic = logic-done). Rhythm = gated-forward (next work must be live-closeable on this machine). 6 commits this session, every one live-verified before being called done. Working tree clean.",
  "completed_work": [
    "5ecc9dbf docs(channels): hermes connectivity deep-dive — docs/guides/channel-connectivity-hermes-deep-dive.md (20 adapters = 3 transport families A/B/C; shared transport seam DRY status; designer-lens pairing UX spec; 4 open decisions in §7).",
    "bccc1629 fix(security): wire execute_bash to docker backend. It was host-exec ONLY — buildDockerRunArgs/approvalRequiredForBackend had no consumer (Proxy Signal: Phase 14 'containment' rested on a function execute_bash never called). resolveExecBackend() (EXEC_BACKEND=docker + reachable -> docker; requested-but-unreachable -> fail-closed, never silent host fallback). network=none live-verified (container has only loopback iface; egress DNS fails). Default stays local (byte-identical).",
    "ef4168f4 fix(security): --user non-root hardening. buildDockerRunArgs added --user (default 65534:65534/nobody, overridable). Live-verified id=uid=65534(nobody), /tmp writable, /etc read-only. Applies to execute_bash + browser.",
    "browser docker containment live-verified 8/8 (read-only root, /tmp noexec, cap-drop ALL CapEff=0, no-new-privileges, no host mounts, docker inspect confirms args). Proved the 'floor' is real before wiring execute_bash onto it.",
    "05639bdf fix(channels): wire SlackConnector into GatewayBot.start(). It was implemented + unit-tested in Phase 12 but NEVER instantiated by the entrypoint (only TG/Discord/Email were). 3rd 'unwired connector'. ack-before-dispatch live-verified on a real workspace (injected wsFactory wrapping a real socket + deliberate 4s slow handler: IN envelope -> OUT ack same id same ms -> dispatch -> no redelivery in the 4s window -> chat.postMessage reply visible).",
    "ee1b1765 refactor(channels): route Slack through channelDispatcher (DRY). Default fetchFn (auth.test/connections.open/chat.postMessage) + default wsFactory (Socket Mode WSS) now use channelDispatcher('SLACK_PROXY',...). undici WebSocket accepts WebSocketInit{dispatcher} at runtime (verified via ctx7 undici docs). Live-verified all three legs reach Slack via dispatcher (no proxy -> primary Agent)."
  ],
  "remaining_work": [
    "Slack residual #2: no allowlist gating — any user who can DM the bot gets a reply (hermes is secure-by-default deny-without-allowlist). Production gap; belongs with the trust-isolation/ADR-47 review.",
    "Slack residual #3 / recurring pattern: WebhookConnector is implemented + unit-tested but STILL unwired into GatewayBot.start() (same class as Slack/execute_bash). Next 'unwired connector' to close; live-closeable by POSTing a crafted webhook request locally.",
    "Channel §7 open decisions (deliverable): (1) add a SOCKS proxy dep for Clash/Shadowrocket/GFW users (channel-http currently HTTP-proxy only); (2) build one shared inbound webhook host (mirror hermes webhook.py) before more Family-B channels; (3) self-healing reconnect wrapper for Family-A connectors.",
    "execute_bash docker is opt-in (EXEC_BACKEND=docker); default is still local host-exec. Decide whether production onboarding should default to docker (doctor already prefers it).",
    "docs/UI-SPEC.md still stale (pre-dates Chat/Sessions/Activity/Settings/Artifacts/Skills) — from the prior ui-console arc.",
    "Other unverified-live carried-forward items now re-classified under the policy: Email/IMAP (no IMAP pair here), macOS/Linux install runs, A2A bridge, registry API verification, iii version pinning — all 'unverified-live + landing condition', not done."
  ],
  "decisions_made": [
    "Live-verification policy: 'done' splits into logic-done (pure logic, unit tests sufficient) vs live-done (crosses an environment boundary -> one real smoke required). live-mandatory surfaces that are only logic-done must be tagged unverified-live, never ticked done. Saved as memory feedback_live_verification_policy.",
    "Rhythm = gated-forward (Option C): next work must be live-closeable on this machine; otherwise it goes to 'unverified-live + landing condition' (the Phase 13 A2A pattern done right).",
    "execute_bash: chose B (wire code to match the ADR-47 claim) over A (downgrade the claim) — the floor was proven real first, so making the claim TRUE beat retreating from it.",
    "Fail-closed when EXEC_BACKEND=docker but docker is unreachable — never silently fall back to host (the docker backend bypasses dangerous-pattern approval).",
    "Query roam-indexed specimens via roam_* MCP tools, not grep/Read on the large files (user instruction; saved to reference_specimens_directory). hermes-agent is the channel-pattern authority.",
    "When adding any new channel: it MUST be wired into packages/gateway-bot/src/index.ts (GatewayBot.start) — the recurring miss. Connectors registered elsewhere (ConnectorRegistry) are only used for outbound delivery."
  ],
  "blockers": [],
  "context": "Memory updated this session: feedback_live_verification_policy, project_execute_bash_containment_unwired (resolved), project_slack_live_closure, project_channel_hermes_deep_dive, reference_specimens_directory (roam usage). Live env: docker 29.4.3 present; Slack workspace 'Memex' (bot @memex U0BA827GTSR) with xapp-/xoxb- tokens in repo-root .env (gitignored); local LLM stack (Qwen3 @8080 + BGE-M3 @8082) from prior session; no IMAP. The full Memex stack was NOT running this session — Slack was live-closed at the transport level in isolation (real SlackConnector.start() + stub onMessage), which is the unverified-live part; the conversation core (dispatchMessage) was already live-verified via Telegram last session.",
  "next_action": "Wire WebhookConnector into GatewayBot.start() (residual #3, same pattern as Slack) and live-verify by POSTing a crafted webhook locally — OR take up a channel §7 decision (SOCKS dep / shared webhook host)."
}
