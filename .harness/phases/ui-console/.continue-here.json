{
  "version": "1.0",
  "phase": "21-console-redesign",
  "task": 1,
  "total_tasks": 1,
  "status": "in_progress",
  "last_updated": "2026-06-14T16:00:00.000Z",
  "current_state": "Onboarding + LLM provider arc complete and pushed (11 commits, fd5fd7c7..4379761c on origin/master). Triggered by the user manually onboarding NVIDIA, which exposed a chain of issues — each fixed at root cause, not patched. The deepest finding (via /systematic-debugging): the runtime OpenAI-compatible provider doubled /v1 in cloud endpoint URLs, 404-ing every strict gateway for chat AND embeddings — masked because all live testing was local (bare-host baseUrl).",
  "completed_work": [
    "92552366 — onboarding asks for API key first, then fetches /models for a pick-list (recommended pinned); new shared/llm/fetch-models.ts (best-effort, never throws)",
    "bcfaa702 — embedding picker filter relaxed from has-default-model to can-embed (3->9 providers); custom prompts a baseUrl as the escape hatch for any OpenAI-compatible embeddings endpoint",
    "ed38d4da — local providers confirm/edit their endpoint (new resolveBaseUrl); the resolved URL both lists models and is persisted; USER_MANUAL adds the Windows localhost->127.0.0.1 IPv6 tip",
    "525e004f — NVIDIA supportsEmbedding=true via baai/bge-m3 (symmetric, no input_type needed; verified against NVIDIA docs)",
    "d4b21be1 — embedding reuse path carries an edited local chat endpoint so embeddings hit the same server as chat",
    "1f6635a9 — reuse-pick option: same provider/key, choose a different embedding model (one-tap default preserved)",
    "b2cd332c — ROOT CAUSE: extracted shared/llm/openai-url.ts :: openaiUrl(baseUrl, route); provider chat+embed and fetch-models share one rule (detect /v\\d, never double /v1); curl-verified; new openai-compatible.provider.test.ts (red->green)",
    "a80336f1 — flag audit close-out: OpenRouter->true (openai/text-embedding-3-small); MiniMax stays false (asymmetric, documented); regression guard locks openrouter+nvidia flags",
    "09ae9626 — docs: ADR-56 supplement D-6/D-7/D-8; configuration.md baseUrl note (versioned or bare both work)",
    "4379761c — onboarding Next-steps uses `memex console` not `npm run dev`; de-leaked console.ts user-facing line"
  ],
  "remaining_work": [
    "AUTHENTICATED LIVE VERIFICATION: a real embedding round-trip through a valid key was NOT done this session (no usable key). URL shapes are curl-confirmed; the authed call is the user's to confirm by re-onboarding NVIDIA and sending a message, checking memory enters the semantic index.",
    "True one-click `memex start`: `memex console` still spawns `npm run dev` under the hood (needs repo root + npm). Packaging belongs to the deploy arc (ROADMAP 15-deploy).",
    "connect/telegram.ts validateBotToken uses plain fetch, not the hardened telegramFetch (channel-http.ts) — restricted-network false negatives; landing condition = hoist channel-http.ts into @graph/shared",
    "ROADMAP 22-workspace-project engine-22 deep integration (per-channel LLM routing, execute_bash cwd->project, forest grouping) — still TODO",
    "Console live-test residue from prior session: dispatch a task, verify first node seeds into /now with P1 effects (zoomToFit/collide/highlight) — blocked on the same authed-LLM path"
  ],
  "decisions_made": [
    "openaiUrl is the single URL rule for all OpenAI-compatible calls; baseUrl may be versioned (OpenAI-SDK style, used by the registry) or bare-host (local) — the rule detects an existing /v\\d segment and never re-adds /v1",
    "Anthropic provider deliberately NOT changed — its base is always bare (api.anthropic.com), not in the doubling class",
    "Embedding flag audit is exhaustive: OpenRouter was the only misclassification; MiniMax/nv-embedqa excluded on purpose (asymmetric, need a query/db type param the symmetric embed() can't send)",
    "Onboarding reuse keeps the one-tap recommended default AND offers reuse-pick — does not force all users through the full model list",
    "First-run guidance presents product commands (memex console), never the dev launcher; operator/dev docs keep npm run dev"
  ],
  "blockers": [
    "No valid LLM key available in-session for an authenticated live embedding/chat verification. The user re-onboarded NVIDIA at the end; the authed round-trip is theirs to confirm."
  ],
  "context": "Authoritative provider-registry decisions: docs/adr/0065-adr56-provider-profile-registry.md (with the 2026-06-14 supplement D-6/D-7/D-8). Per-item root-cause log: .harness/implementation-notes.md (multiple 2026-06-14 sections). Memory: project_onboarding_provider_arc. Provider registry SSOT: packages/shared/src/llm/provider-profiles.ts. URL rule: packages/shared/src/llm/openai-url.ts. Onboarding TUI: packages/cli/src/onboard.ts. Working tree has pre-existing unrelated edits (memex.mjs/memex-terminal.mjs from user onboarding) — not this session's.",
  "next_action": "Have the user re-onboard NVIDIA, run `memex console`, send a chat message, and confirm the authenticated /embeddings round-trip works (memory enters the semantic index) — the one residual the curl tests couldn't cover."
}
