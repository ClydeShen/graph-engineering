---
phase: 03-pattern-discovery
plan: 07
type: execute
wave: 4
depends_on: ["03-02", "03-03", "03-04", "03-05", "03-06"]
files_modified:
  - packages/workers/src/patterns/gate4.integration.test.ts
  - packages/gateway/src/routes/gate4-mcp.integration.test.ts
autonomous: true
requirements: [GATE4-1, GATE4-2, GATE4-3, GATE4-4, GATE4-5]
must_haves:
  truths:
    - "All five Gate 4 success criteria have an automated test that passes against a live DB"
    - "GATE4-1: two topologically equivalent cross-domain scopes have topology_embedding cosine > 0.90"
    - "GATE4-2: CrossScopePatternDiscoveryWorker writes cross_domain_cluster_id for matching template pairs"
    - "GATE4-3: child scope_closed propagates to parent via sub_scope_resolved and the parent task advances"
    - "GATE4-4: an MCP client can call spawn_subtask + claim_next_task + complete_task end-to-end"
    - "GATE4-5: FrontierScheduler dispatches a task by skill match, not arbitrary assignment"
  artifacts:
    - path: "packages/workers/src/patterns/gate4.integration.test.ts"
      provides: "GATE4-1, GATE4-2, GATE4-3, GATE4-5 integration assertions"
      contains: "GATE4"
    - path: "packages/gateway/src/routes/gate4-mcp.integration.test.ts"
      provides: "GATE4-4 end-to-end MCP client integration assertion"
      contains: "spawn_subtask"
  key_links:
    - from: "packages/workers/src/patterns/gate4.integration.test.ts"
      to: "discoverClusters + nestScope/resolveSubScope + frontier skill match"
      via: "exercises each Stream A criterion against a live partitioned DB"
      pattern: "cross_domain_cluster_id"
---

<objective>
Author the Gate 4 integration test suite that proves all five Phase 3 success criteria pass end-to-end against a live PostgreSQL instance. This is the phase gate: full suite green before `/gsd-verify-work`. The plan consolidates the criteria into two integration files (workers-side for Stream A + GATE4-5, gateway-side for the MCP GATE4-4 round trip) and reuses the Plan 01 scaffolds where they exist.

Purpose: The earlier plans turned their own unit/scaffold tests green; this plan adds the cross-cutting, DB-backed integration proofs that the criteria hold together (e.g. nested-scope round trip spans Control Plane + Worker; skill-match dispatch spans agent_registry + FrontierScheduler).

Output: `gate4.integration.test.ts` (GATE4-1/2/3/5) and `gate4-mcp.integration.test.ts` (GATE4-4), DB-gated with skipIf, all passing when DATABASE_URL is set.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-pattern-discovery/03-RESEARCH.md
@.planning/phases/03-pattern-discovery/03-CONTEXT.md
@docs/adr/0025-adr23-nested-scope-propagation.md
@docs/adr/0027-adr25-cross-domain-topology-algorithm.md
---
# Gate 4 success criteria (CONTEXT.md §Phase 3 Gate): GATE4-1..GATE4-5 (all five must pass).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stream A + skill-match integration tests (GATE4-1/2/3/5)</name>
  <files>packages/workers/src/patterns/gate4.integration.test.ts</files>

  <read_first>
    packages/workers/src/memory/gate3.integration.test.ts — the Phase 2 gate integration test structure: skipIf(skip) DB gating, scope/partition setup via nestScope, seeding rows, partition cleanup; mirror this exactly for the Gate 4 file
    packages/workers/src/patterns/cross-scope.ts — discoverClusters + assignClusters exports (Plan 02) to drive GATE4-1/GATE4-2
    packages/control-plane/src/nesting.ts — nestScope + createSubScope + resolveSubScope (Plan 04) to drive GATE4-3
    packages/control-plane/src/pulse-fetch.ts — the sub_scope_resolved topic (Plan 04) and how the resolved signal reaches SubScopeResultWorker for the GATE4-3 round trip
    packages/workers/src/nested/sub-scope-result.worker.ts — SubScopeResultWorker.onSubScopeResolved (Plan 06) to complete the GATE4-3 parent-advance assertion
    packages/workers/src/scheduler/frontier.worker.ts — onFrontierChanged + the skill-match filter (Plan 03) and agent_registry seeding for GATE4-5
    packages/workers/src/memory/wl-embedding.ts — computeWLEmbedding for constructing two cross-domain isomorphic graphs (GATE4-1)
    .planning/phases/03-pattern-discovery/03-RESEARCH.md §"Phase Requirements → Test Map" — exact criterion-to-assertion mapping
  </read_first>

  <action>
  Create `packages/workers/src/patterns/gate4.integration.test.ts`, DB-gated with `skipIf(!process.env.DATABASE_URL)`, mirroring gate3.integration.test.ts setup/teardown (create scopes via nestScope, clean up partitions afterward).

  GATE4-1: build two topologically equivalent graphs whose node ids/labels represent different domains but share identical structure; compute both topology_embeddings via computeWLEmbedding; assert cosine similarity > 0.90.

  GATE4-2: seed two procedural_memory rows with near-identical topology_embedding and distant intent_embedding (and a third structurally-different row as a negative control); run discoverClusters(pool); assert the two similar/distant rows share a non-NULL cross_domain_cluster_id and the negative control does not join them. Assert idempotency: a second discoverClusters run does not change the assignments.

  GATE4-3: create a parent scope and a child sub-scope via createSubScope; close the child (write scope_closed in the child partition / drive the watchdog path); invoke resolveSubScope to inject sub_scope_resolved into the parent; route it to SubScopeResultWorker.onSubScopeResolved; assert a memory_updated with a result_summary lands in the parent partition referencing the child_scope_id, and the parent's spawning task is no longer pending (advanced/completed).

  GATE4-5: seed agent_registry with an active fresh-heartbeat agent having skills ['typescript']; insert a frontier task with required_skills ['typescript'] and a second task with required_skills ['nonexistent-skill']; run onFrontierChanged; assert the typescript task is marked pending_dispatch and the nonexistent-skill task remains pending_scheduling (dispatched by skill match, not arbitrary).

  Each test names its GATE4 criterion in the test title. Reuse helpers from gate3 where possible; do not duplicate partition-cleanup logic if a shared helper exists.
  </action>

  <verify>
    <automated>npx vitest run packages/workers/src/patterns/gate4.integration.test.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>

  <acceptance_criteria>
  - GATE4-1 test asserts cosine(topology_embedding_A, topology_embedding_B) > 0.90 for two cross-domain isomorphic graphs
  - GATE4-2 test asserts the matched pair shares a non-NULL cross_domain_cluster_id, the negative control does not join, and a second run is idempotent
  - GATE4-3 test asserts a result_summary memory_updated lands in the parent referencing child_scope_id and the parent spawning task advanced past pending
  - GATE4-5 test asserts the skill-matched task is pending_dispatch and the unmatched-skill task stays pending_scheduling
  - All four assertions pass when DATABASE_URL is set; the file is skipped (not failed) when DATABASE_URL is absent
  - `npm run typecheck` exits 0
  </acceptance_criteria>

  <done>gate4.integration.test.ts proves GATE4-1, GATE4-2, GATE4-3, and GATE4-5 end-to-end against a live partitioned database.</done>
</task>

<task type="auto">
  <name>Task 2: MCP end-to-end integration test (GATE4-4)</name>
  <files>packages/gateway/src/routes/gate4-mcp.integration.test.ts</files>

  <read_first>
    packages/gateway/src/routes/mcp.test.ts — the Plan 01 scaffold turned green in Plan 05; this Gate test is the fuller end-to-end version (a real MCP client driving spawn→claim→complete and asserting ledger state)
    packages/gateway/src/routes/mcp.ts — buildMcpRoute mount + transport (Plan 05) and the /mcp/messages POST entry the test drives
    packages/gateway/src/mcp/server.ts — the 7 tool handlers (Plan 05); the test calls spawn_subtask, claim_next_task, complete_task
    packages/gateway/src/index.ts — buildApp(pool, ddlPool, wMax) to construct the app under test in-process
    packages/gateway/src/routes/health.test.ts — in-process app exercise pattern (app.fetch / request construction) to drive JSON-RPC POSTs
    .planning/phases/03-pattern-discovery/03-CONTEXT.md §Gate 4 criterion 4 — "External Agent (MCP client) can call spawn_subtask + claim_next_task + complete_task against a live graph-os instance"
  </read_first>

  <action>
  Create `packages/gateway/src/routes/gate4-mcp.integration.test.ts`, DB-gated with `skipIf(!process.env.DATABASE_URL)`. Build the app via buildApp with a real pool + ddlPool, create a scope (nestScope or POST /v1/scopes), then drive the MCP tools as an MCP client would via POST /mcp/messages JSON-RPC calls:

  1. Register an agent (register_agent) with skills ['typescript'].
  2. spawn_subtask with required_skills ['typescript'] → assert a task_spawned event exists in the ledger and a task_id is returned; assert that spawning with assigned_agent_id in payload is rejected (D-1).
  3. claim_next_task with skills ['typescript'] → assert it returns the spawned task and transitions it to processing (SKIP LOCKED).
  4. complete_task with a result → assert a memory_updated event lands and the task is marked done.
  5. tools/list → assert all 7 tool names are present.

  Assert no event type outside the five canonical EVENT_TYPES was written during the round trip. Clean up the scope partition in teardown.
  </action>

  <verify>
    <automated>npx vitest run packages/gateway/src/routes/gate4-mcp.integration.test.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>

  <acceptance_criteria>
  - tools/list returns exactly the 7 tool names
  - spawn_subtask creates a task_spawned event and returns a task_id; a spawn carrying assigned_agent_id is rejected (D-1)
  - claim_next_task returns the spawned task and transitions it to processing
  - complete_task writes a memory_updated and marks the task done
  - No event with a type outside the five canonical EVENT_TYPES is written during the round trip
  - The file is skipped (not failed) without DATABASE_URL; passes with it
  - `npm run typecheck` exits 0
  </acceptance_criteria>

  <done>gate4-mcp.integration.test.ts proves an MCP client can run spawn_subtask → claim_next_task → complete_task end-to-end against a live graph-os instance (GATE4-4).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → live DB | integration tests create/drop partitions and seed registry rows on a dev/test database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07-01 | Tampering | integration tests leaving orphan partitions/rows | mitigate | each test cleans up its scope partition and seeded agent_registry rows in teardown (mirror gate3 cleanup) |
| T-03-07-02 | Denial of Service | tests run against a production DB | mitigate | skipIf(!DATABASE_URL) + tests target a dev/test DATABASE_URL only; never pointed at production in CI config |
| T-03-07-SC | Tampering | npm/pip/cargo installs | accept | No new packages — tests use vitest + existing deps |
</threat_model>

<verification>
- `npx vitest run packages/workers/src/patterns/gate4.integration.test.ts packages/gateway/src/routes/gate4-mcp.integration.test.ts` green with DATABASE_URL set
- Full suite `npx vitest run` green (no regressions across Phase 1/2/3)
- `npm run typecheck` exits 0
- All five Gate 4 criteria have a passing automated assertion
</verification>

<success_criteria>
1. GATE4-1 through GATE4-5 each have an automated, DB-backed test that passes.
2. The full test suite is green (no Phase 1/2 regressions).
3. The phase is ready for `/gsd-verify-work`.
</success_criteria>

<output>
Create `.planning/phases/03-pattern-discovery/03-07-SUMMARY.md` when done.
</output>
