---
phase: 03-pattern-discovery
plan: 06
type: execute
wave: 3
depends_on: ["03-04", "03-05"]
files_modified:
  - packages/workers/src/nested/sub-scope-result.worker.ts
  - packages/workers/src/index.ts
  - packages/workers/src/nested/sub-scope-result.worker.test.ts
autonomous: true
requirements: [GATE4-3]
must_haves:
  truths:
    - "SubScopeResultWorker is woken by sub_scope_resolved via the graph::scope::sub_scope_resolved topic"
    - "It reads the child scope tail node via child_final_version_hash, calls the LLM to synthesize a result summary, and writes memory_updated to the parent"
    - "The parent task node advances: the synthesized memory_updated lets the parent topology mark the spawning task completed"
    - "All internal Workers register an AgentCard in agent_registry at boot (D-2 universalization)"
    - "Internal Worker AgentCards declare protocol 'iii' and their accepted skills"
  artifacts:
    - path: "packages/workers/src/nested/sub-scope-result.worker.ts"
      provides: "SubScopeResultWorker: sub_scope_resolved → LLM synthesis → parent memory_updated"
      exports: ["SubScopeResultWorker", "SUB_SCOPE_RESULT_TRIGGER_CONFIG"]
      min_lines: 40
    - path: "packages/workers/src/index.ts"
      provides: "SubScopeResultWorker registration + internal Worker AgentCard bootstrap"
      contains: "agent_registry"
  key_links:
    - from: "packages/workers/src/nested/sub-scope-result.worker.ts"
      to: "parent partition execution_event_log"
      via: "occWrite eventType memory_updated with result_summary payload"
      pattern: "memory_updated"
    - from: "packages/workers/src/index.ts"
      to: "agent_registry"
      via: "boot INSERT of internal Worker AgentCards (ON CONFLICT DO NOTHING)"
      pattern: "agent_registry"
---

<objective>
Complete GATE4-3 by adding the parent-side `SubScopeResultWorker`: woken via the `graph::scope::sub_scope_resolved` topic (routed by Plan 04's Pulse-Fetch branch), it reads the child scope's final node (via `child_final_version_hash`), calls the LLM provider to synthesize a result summary, and writes a standard `memory_updated` to the parent partition so the parent's spawning task advances to completed. Also implement the D-2 universalization requirement: every internal Worker registers an AgentCard in `agent_registry` at boot.

Purpose: SubScopeResultWorker is the semantic-merge half of nested scopes (ADR 23) — the LLM call lives in the Worker layer, not the Control Plane. D-2 (LOCKED) requires internal Workers to be first-class registry participants so FrontierScheduler can route skill-matched tasks to them, the same as external Agents.

Output: `sub-scope-result.worker.ts` (new), index.ts registration on the sub-scope topic + the boot-time AgentCard bootstrap, and the worker's unit test. With this plan, GATE4-3 (child scope_closed → sub_scope_resolved → parent advances) is end-to-end complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-pattern-discovery/03-RESEARCH.md
@.planning/phases/03-pattern-discovery/03-CONTEXT.md
@docs/adr/0025-adr23-nested-scope-propagation.md
@.harness/phases/side-branch/DESIGN.md
---
# SubScopeResultWorker subscribes to topic graph::scope::sub_scope_resolved (Plan 04), NOT to an event-type enum.
# D-2: ALL internal Workers register an AgentCard (protocol 'iii') in agent_registry at boot.
---
# Topic name and trigger_task_id passing mechanism are recorded in .harness/implementation-notes.md by Plan 04 — read it before implementing.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement SubScopeResultWorker</name>
  <files>packages/workers/src/nested/sub-scope-result.worker.ts, packages/workers/src/nested/sub-scope-result.worker.test.ts</files>

  <read_first>
    packages/workers/src/memory/semantic.worker.ts — the canonical pattern for an LLM-using Worker: how it receives the injected LLMProvider, calls it to distill text, and writes results via occWrite; mirror this structure (constructor(pool, llm), trigger config export, durable:subscriber topic)
    packages/workers/src/memory/episodic.worker.ts — the durable:subscriber TriggerConfig export shape (function_id + config.topic) to mirror for SUB_SCOPE_RESULT_TRIGGER_CONFIG with topic graph::scope::sub_scope_resolved
    docs/adr/0025-adr23-nested-scope-propagation.md §SubScopeResultWorker — the four steps: read child tail node by child_final_version_hash, LLM-synthesize summary, write memory_updated to parent with predecessor_hash pointing at the parent's task_spawned node, then destroy context window
    packages/shared/src/occ-write.ts — occWrite signature; eventType 'memory_updated'; the result write targets the PARENT scope partition
    .planning/phases/03-pattern-discovery/03-RESEARCH.md §Pitfall 3 — the worker subscribes by topic (graph::scope::sub_scope_resolved), not by event_type; this avoids iii-sdk rejecting a non-canonical type
    .harness/implementation-notes.md — Plan 04's recorded sub_scope_resolved payload keys (child_scope_id, trigger_task_id, child_final_version_hash) and the exact topic string
  </read_first>

  <behavior>
    - onSubScopeResolved(payload): given {child_scope_id, trigger_task_id, child_final_version_hash, parent_scope_id}, reads the child scope's final node by child_final_version_hash, calls llm to synthesize a result_summary, occWrites a memory_updated to the PARENT scope with payload {sub_scope_resolved: child_scope_id, result_summary, child_final_version_hash}
    - The parent memory_updated predecessor_hash points at the parent's spawning task_spawned node (trigger_task_id) so the parent topology advances that task to completed
    - If the child final node cannot be found, the worker writes a memory_updated marking the sub-scope as resolved-with-error (does not hang the parent)
    - SUB_SCOPE_RESULT_TRIGGER_CONFIG declares a durable:subscriber on topic graph::scope::sub_scope_resolved
  </behavior>

  <action>
  Create `packages/workers/src/nested/sub-scope-result.worker.ts`. Export `SUB_SCOPE_RESULT_TRIGGER_CONFIG` (durable:subscriber, function_id 'graph::scope::sub-scope-result', config.topic = the exact string Plan 04 used: `graph::scope::sub_scope_resolved` — confirm via implementation-notes.md). Export class `SubScopeResultWorker` with `constructor(pool: Pool, llm: LLMProvider)` mirroring SemanticMemoryWorker.

  `onSubScopeResolved(payload)`: parse {child_scope_id, trigger_task_id, child_final_version_hash, parent_scope_id}. Read the child scope's final node by `child_final_version_hash` from the child partition (SELECT payload/content FROM execution_event_log WHERE scope_id=child AND version_hash=child_final_version_hash). Call the LLM provider to synthesize a concise result_summary from the child's final content. Then occWrite a `memory_updated` to the PARENT scope: entityId fresh, predecessorHash = the parent's task_spawned node hash for trigger_task_id (look it up from the parent partition), payload {sub_scope_resolved: child_scope_id, result_summary, child_final_version_hash}, eventType 'memory_updated'. This is the standard Worker write that lets the parent topology mark the spawning task completed (ADR 23 step 3).

  Error path: if the child final node is not found, synthesize no summary and write a memory_updated with {sub_scope_resolved: child_scope_id, result_summary: null, error: 'child_final_node_not_found'} so the parent does not hang (D-6 backstop alignment).

  Add unit test sub-scope-result.worker.test.ts mirroring semantic.worker.test.ts: mock the LLM provider and pool; assert (a) onSubScopeResolved reads the child node by child_final_version_hash, (b) calls the LLM with the child content, (c) occWrites a memory_updated to the parent scope with result_summary in the payload, (d) the not-found path writes an error memory_updated and does not throw.
  </action>

  <verify>
    <automated>npx vitest run packages/workers/src/nested/sub-scope-result.worker.test.ts</automated>
    <automated>npm run typecheck</automated>
    <automated>grep -c "graph::scope::sub_scope_resolved" packages/workers/src/nested/sub-scope-result.worker.ts</automated>
  </verify>

  <acceptance_criteria>
  - SUB_SCOPE_RESULT_TRIGGER_CONFIG is a durable:subscriber whose config.topic is exactly `graph::scope::sub_scope_resolved` (matches Plan 04)
  - onSubScopeResolved reads the child node by child_final_version_hash and calls the injected LLM provider
  - It occWrites a memory_updated to the PARENT scope with a result_summary in the payload (grep confirms eventType memory_updated)
  - The child-node-not-found path writes an error memory_updated and does not throw
  - `npm run typecheck` exits 0 and the worker unit test passes
  </acceptance_criteria>

  <done>SubScopeResultWorker subscribes to the sub-scope topic, synthesizes a child-scope result via the LLM, and writes a memory_updated to the parent so the spawning task advances — completing the GATE4-3 round trip.</done>
</task>

<task type="auto">
  <name>Task 2: Register SubScopeResultWorker + bootstrap internal Worker AgentCards (D-2)</name>
  <files>packages/workers/src/index.ts</files>

  <read_first>
    packages/workers/src/index.ts — the single boot entry point: how each Worker is constructed (pool, llmProvider injected), how registerFunction + registerTrigger are paired, and the existing ordering. SubScopeResultWorker registers the same way; the AgentCard bootstrap runs once at boot after the pool is created.
    packages/workers/src/nested/sub-scope-result.worker.ts — SubScopeResultWorker + SUB_SCOPE_RESULT_TRIGGER_CONFIG exports (from Task 1)
    migrations/007-pattern-discovery-mcp.sql — agent_registry columns + protocol CHECK (must be 'iii' for internal Workers) + the ON CONFLICT target (agent_id PRIMARY KEY)
    .planning/phases/03-pattern-discovery/03-CONTEXT.md §D-2 — AgentCard universalization: internal Workers register with skills they accept; minimum card shape (agent_id, name, description, skills[], protocol, endpoint, version)
    .harness/phases/side-branch/DESIGN.md §D-2 — internal Workers (Discovery, Frontier, Episodic, etc.) are registry participants
    packages/gateway/src/mcp/server.ts — the register_agent INSERT shape (Plan 05) to keep the internal bootstrap INSERT consistent (same columns, ON CONFLICT semantics)
  </read_first>

  <action>
  Register SubScopeResultWorker in index.ts mirroring the SemanticMemoryWorker block: `new SubScopeResultWorker(pool, llmProvider)`, registerFunction('graph::scope::sub-scope-result', handler that parses the payload and calls onSubScopeResolved), then `worker.registerTrigger(SUB_SCOPE_RESULT_TRIGGER_CONFIG)`.

  Add a boot-time internal Worker AgentCard bootstrap (D-2). After the pool is created and before/around worker registration, run an idempotent registration that INSERTs one agent_registry row per internal Worker with protocol 'iii', a stable agent_id (deterministic per Worker name so re-boots do not create duplicates — e.g. a fixed UUID per Worker or ON CONFLICT (agent_id) DO NOTHING), skills declaring what each Worker accepts, and agent_card_json holding the full card. Cover the internal Workers that accept skill-routed tasks (at minimum: frontier scheduler, episodic, semantic, procedural, conflict-resolver, sub-scope-result, pattern-discovery). Use `INSERT INTO agent_registry (...) VALUES (...) ON CONFLICT (agent_id) DO NOTHING` (or DO UPDATE last_heartbeat) so boot is idempotent.

  Keep the change surgical and consistent with the register_agent column shape from Plan 05. Skills granularity is Claude's discretion (CONTEXT.md) — choose a coarse, consistent vocabulary and note it in .harness/implementation-notes.md.
  </action>

  <verify>
    <automated>npm run typecheck</automated>
    <automated>grep -c "SUB_SCOPE_RESULT_TRIGGER_CONFIG" packages/workers/src/index.ts</automated>
    <automated>grep -c "agent_registry" packages/workers/src/index.ts</automated>
    <automated>npx vitest run packages/workers/src</automated>
  </verify>

  <acceptance_criteria>
  - index.ts registers 'graph::scope::sub-scope-result' with registerFunction + registerTrigger(SUB_SCOPE_RESULT_TRIGGER_CONFIG)
  - Boot performs an idempotent INSERT into agent_registry for each internal Worker with protocol 'iii' and ON CONFLICT handling (re-boot creates no duplicates)
  - Internal AgentCards declare a skills array and a stable agent_id per Worker
  - `grep -c "agent_registry" packages/workers/src/index.ts` returns 1 or more
  - `npm run typecheck` exits 0 and packages/workers/src tests pass
  </acceptance_criteria>

  <done>SubScopeResultWorker is wired into the boot entry point on the sub-scope topic, and every internal Worker registers an idempotent AgentCard in agent_registry at boot per D-2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sub_scope_resolved signal → SubScopeResultWorker | control-plane-written signal (trusted source) carrying child scope pointers |
| child scope content → LLM provider | child final node content sent to the embedding/completion endpoint |
| boot → agent_registry | internal AgentCard self-registration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | Denial of Service | child final node missing → parent hangs forever | mitigate | not-found path writes an error memory_updated so the parent task advances; D-6 Watchdog TTL is the backstop |
| T-03-06-02 | Tampering | duplicate AgentCards on re-boot | mitigate | ON CONFLICT (agent_id) DO NOTHING with stable per-Worker agent_id makes boot registration idempotent |
| T-03-06-03 | Information Disclosure | result_summary leaking child scope internals to parent | accept | By design (ADR 23): the parent is meant to receive the synthesized child result; summary is the intended cross-scope hand-off |
| T-03-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages — pure TypeScript + SQL on existing pg + LLMProvider |
</threat_model>

<verification>
- `npx vitest run packages/workers/src` green
- `npm run typecheck` exits 0
- Boot registration is idempotent (re-running produces no duplicate agent_registry rows for internal Workers)
- End-to-end (with DB): child scope_closed → Plan 04 sub_scope_resolved → this worker → parent memory_updated → parent task completed (validated fully in Plan 07 Gate test)
</verification>

<success_criteria>
1. SubScopeResultWorker wakes on graph::scope::sub_scope_resolved, synthesizes via LLM, writes memory_updated to the parent.
2. The not-found path does not hang the parent.
3. All internal Workers register idempotent AgentCards (protocol 'iii') at boot (D-2).
4. GATE4-3 round trip end-to-end complete.
</success_criteria>

<output>
Create `.planning/phases/03-pattern-discovery/03-06-SUMMARY.md` when done.
</output>
