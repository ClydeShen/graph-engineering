---
phase: 02-memory-retrieval
plan: 04
type: execute
wave: 2
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - packages/workers/src/memory/synthesizer.worker.ts
  - packages/workers/src/memory/synthesizer.worker.test.ts
  - packages/workers/src/index.ts
autonomous: true
requirements: [MEM-03]
must_haves:
  truths:
    - "MemorySynthesizerWorker exports three cron trigger configs: SYNTHESIZER_CRON_TRIGGER (2AM), DECAY_CRON_TRIGGER (3AM), TTL_CRON_TRIGGER (4AM)"
    - "All three triggers use 7-field iii-cron format: '0 0 H * * * *'"
    - "MemorySynthesizerWorker does NOT extend Worker ABC — holds Pool and LLMProvider in constructor"
    - "runDecay() executes UPDATE procedural_memory SET superseded_by = id WHERE reinforcement_count = 0 AND last_used_at < NOW() - INTERVAL '90 days' AND superseded_by IS NULL"
    - "runTtlPurge() executes DELETE FROM working_memory WHERE created_at < NOW() - INTERVAL '24 hours'"
    - "runSynthesis() queries episodic_memory, calls LLM to distill, builds nodes/edges from episodic row sequence, returns discriminated union: { skipped: true } | { skipped: false; scope_id; intent_description; template_graph; nodes; edges }; index.ts callback triggers 'graph::memory::procedural' via worker.trigger(TriggerAction.Void()) when skipped:false — this is the synthesizer→procedural publish link"
    - "Three separate function IDs registered: graph::memory::synthesizer, graph::memory::decay, graph::memory::ttl"
  artifacts:
    - path: "packages/workers/src/memory/synthesizer.worker.ts"
      provides: "MemorySynthesizerWorker class + three cron trigger config exports"
      exports: ["MemorySynthesizerWorker", "SYNTHESIZER_CRON_TRIGGER", "DECAY_CRON_TRIGGER", "TTL_CRON_TRIGGER"]
    - path: "packages/workers/src/memory/synthesizer.worker.test.ts"
      provides: "Unit tests covering runDecay (G3-6), runTtlPurge, runSynthesis early-exit"
      contains: "superseded_by = id"
    - path: "packages/workers/src/index.ts"
      provides: "Registration of all three cron functions + triggers"
      contains: "graph::memory::synthesizer"
  key_links:
    - from: "packages/workers/src/memory/synthesizer.worker.ts"
      to: "procedural_memory"
      via: "pool.query UPDATE SET superseded_by = id"
      pattern: "superseded_by = id"
    - from: "packages/workers/src/memory/synthesizer.worker.ts"
      to: "working_memory"
      via: "pool.query DELETE FROM working_memory"
      pattern: "DELETE FROM working_memory"
    - from: "packages/workers/src/index.ts"
      to: "MemorySynthesizerWorker"
      via: "worker.registerFunction('graph::memory::synthesizer', ...)"
      pattern: "graph::memory::synthesizer"
---

<objective>
Implement MemorySynthesizerWorker — three iii-cron triggers (2AM synthesis, 3AM Ebbinghaus decay, 4AM working_memory TTL) that maintain memory health over time. The Ebbinghaus decay SQL directly satisfies Gate 3 criterion G3-6.

Purpose: Without the decay cron, procedural_memory accumulates stale patterns indefinitely and G3-6 cannot pass. Without TTL purge, working_memory grows unbounded. Synthesis cron is the daily batch path for producing procedural templates.

Output: `synthesizer.worker.ts` (worker + three trigger configs), `synthesizer.worker.test.ts`, and extension of `packages/workers/src/index.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-memory-retrieval/02-RESEARCH.md
@.planning/phases/02-memory-retrieval/02-PATTERNS.md
@.harness/phases/03-execute/PHASE2-DESIGN.md
@.planning/phases/02-memory-retrieval/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create MemorySynthesizerWorker with test</name>
  <files>packages/workers/src/memory/synthesizer.worker.ts, packages/workers/src/memory/synthesizer.worker.test.ts</files>

  <read_first>
    node_modules/@graph/workers/src/patterns/discover.worker.ts — exact cron worker pattern (no ABC, base_priority = 1, Pool in constructor)
    packages/workers/src/memory/semantic.worker.ts (from 02-03) — LLMProvider injection pattern, llm.chat() usage
    node_modules/@graph/shared/src/llm/provider.interface.ts — LLMProvider.chat() signature
    migrations/006-memory-extensions.sql (from 02-01) — confirm reinforcement_count, last_used_at, superseded_by exist on procedural_memory
    migrations/003-memory-tables.sql — confirm working_memory.created_at column exists
    .planning/phases/02-memory-retrieval/02-PATTERNS.md §synthesizer.worker.ts — full class shape with three cron triggers
  </read_first>

  <behavior>
  Tests to write before implementation (RED phase):
  - Test 1: runDecay() calls pool.query with SQL containing 'superseded_by = id' and WHERE conditions for reinforcement_count = 0 and INTERVAL '90 days'
  - Test 2: runDecay() — mock pool throws → error propagates (no silent swallow)
  - Test 3: runTtlPurge() calls pool.query with DELETE FROM working_memory WHERE created_at < NOW() - INTERVAL '24 hours'
  - Test 4: runSynthesis() returns { skipped: true } when episodic_memory returns 0 rows (early exit — no LLM call)
  - Test 5: runSynthesis() returns { skipped: false, scope_id, intent_description, template_graph, nodes, edges } when episodic rows exist and LLM returns JSON; nodes.length === rows.length; edges.length === rows.length - 1
  - Test 6: SYNTHESIZER_CRON_TRIGGER.config.expression is '0 0 2 * * * *'; DECAY_CRON_TRIGGER is '0 0 3 * * * *'; TTL_CRON_TRIGGER is '0 0 4 * * * *'
  </behavior>

  <action>
  Write `packages/workers/src/memory/synthesizer.worker.test.ts` first (RED). Mock pool returning controlled responses. Mock llm with vi.fn(). Run test — must fail (RED confirmed).

  Then write `packages/workers/src/memory/synthesizer.worker.ts` (GREEN):

  Imports:
  - `import type { Pool } from 'pg'`
  - `import { writeGuard } from '@graph/shared'`
  - `import type { LLMProvider } from '@graph/shared'`

  Three cron trigger config exports (7-field iii-cron format — sec min hour day month weekday year):
  ```
  export const SYNTHESIZER_CRON_TRIGGER = {
    type: 'cron' as const,
    function_id: 'graph::memory::synthesizer',
    config: { expression: '0 0 2 * * * *' },
  } as const;

  export const DECAY_CRON_TRIGGER = {
    type: 'cron' as const,
    function_id: 'graph::memory::decay',
    config: { expression: '0 0 3 * * * *' },
  } as const;

  export const TTL_CRON_TRIGGER = {
    type: 'cron' as const,
    function_id: 'graph::memory::ttl',
    config: { expression: '0 0 4 * * * *' },
  } as const;
  ```

  Class: `export class MemorySynthesizerWorker` — does NOT extend any ABC. Matches PatternDiscoveryWorker pattern exactly.
  ```
  export class MemorySynthesizerWorker {
    readonly base_priority = 1;
    private readonly pool: Pool;
    private readonly llm: LLMProvider;

    constructor(pool: Pool, llm: LLMProvider) {
      this.pool = pool;
      this.llm = llm;
    }
  ```

  Method `async runSynthesis(): Promise<{ skipped: boolean }>`:
  ```
  const { rows } = await this.pool.query<{ scope_id: string; content: string }>(
    `SELECT scope_id, content FROM episodic_memory
     WHERE created_at > NOW() - INTERVAL '25 hours'
     ORDER BY created_at ASC
     LIMIT 100`,
  );
  if (rows.length === 0) return { skipped: true };

  const combined = rows.map((r) => r.content).join('\n');
  // LLM CALL — ADR 22 (batch distillation; cannot be deterministic)
  const summary = await this.llm.chat([
    { role: 'system', content: 'Distill execution traces into reusable workflow templates. Output JSON with fields: intent_description (string), steps (string[]).' },
    { role: 'user', content: writeGuard(combined) },
  ]);

  // Parse LLM output — fallback gracefully if not valid JSON
  let parsed: { intent_description?: string; steps?: string[] } = {};
  try { parsed = JSON.parse(summary) as typeof parsed; } catch { /* non-JSON — use raw output */ }

  const intentDescription = parsed.intent_description ?? summary;
  // Build minimal sequential topology from episodic row order for WL kernel (Plan 05)
  const nodes = rows.map((_, i) => ({ id: `node-${i}`, event_type: 'episodic_trace' }));
  const edges = rows.slice(1).map((_, i) => ({ source: `node-${i}`, target: `node-${i + 1}` }));

  return {
    skipped: false as const,
    scope_id: rows[0].scope_id,
    intent_description: intentDescription,
    template_graph: { steps: parsed.steps ?? [] },
    nodes,
    edges,
  };
  ```

  Method `async runDecay(): Promise<void>`:
  ```
  // Ebbinghaus decay — pure SQL, NO LLM call (ADR 20 Task 3)
  await this.pool.query(`
    UPDATE procedural_memory
    SET superseded_by = id
    WHERE reinforcement_count = 0
      AND last_used_at < NOW() - INTERVAL '90 days'
      AND superseded_by IS NULL
  `);
  ```

  Method `async runTtlPurge(): Promise<void>`:
  ```
  // working_memory 24h TTL — pure SQL, NO LLM call
  await this.pool.query(
    `DELETE FROM working_memory WHERE created_at < NOW() - INTERVAL '24 hours'`,
  );
  ```

  No try/catch in any method — errors propagate to iii-sdk caller.
  </action>

  <verify>
    <automated>npx vitest run packages/workers/src/memory/synthesizer.worker.test.ts --reporter=verbose</automated>
    <automated>npm run typecheck 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
  - All 6 tests pass
  - `npm run typecheck` exits 0
  - `synthesizer.worker.ts` exports `MemorySynthesizerWorker`, `SYNTHESIZER_CRON_TRIGGER`, `DECAY_CRON_TRIGGER`, `TTL_CRON_TRIGGER`
  - `runSynthesis()` return type is a discriminated union: `{ skipped: true }` | `{ skipped: false; scope_id: string; intent_description: string; template_graph: ...; nodes: ...; edges: ... }`
  - `SYNTHESIZER_CRON_TRIGGER.type` is `'cron'` (as const)
  - `DECAY_CRON_TRIGGER.config.expression` is `'0 0 3 * * * *'`
  - `TTL_CRON_TRIGGER.config.expression` is `'0 0 4 * * * *'`
  - `MemorySynthesizerWorker` does NOT import from any Worker ABC
  - `runDecay()` SQL contains `superseded_by = id` and `reinforcement_count = 0` and `INTERVAL '90 days'`
  - `runTtlPurge()` SQL is `DELETE FROM working_memory WHERE created_at < NOW() - INTERVAL '24 hours'`
  </acceptance_criteria>

  <done>synthesizer.worker.ts and synthesizer.worker.test.ts exist; all tests pass; typecheck exits 0</done>
</task>

<task type="auto">
  <name>Task 2: Register MemorySynthesizerWorker in workers/index.ts</name>
  <files>packages/workers/src/index.ts</files>

  <read_first>
    packages/workers/src/index.ts — current state after 02-03; read full file; check if llmProvider already declared
    packages/workers/src/memory/synthesizer.worker.ts (created in Task 1) — confirm export names
    .planning/phases/02-memory-retrieval/02-PATTERNS.md §packages/workers/src/index.ts — registration block shapes for all three cron functions
  </read_first>

  <action>
  Edit `packages/workers/src/index.ts`. Add:

  Import additions (after existing memory imports):
  ```
  import { randomUUID } from 'crypto';
  import { TriggerAction } from 'iii-sdk';
  import {
    MemorySynthesizerWorker,
    SYNTHESIZER_CRON_TRIGGER,
    DECAY_CRON_TRIGGER,
    TTL_CRON_TRIGGER,
  } from './memory/synthesizer.worker.js';
  ```

  Note: `llmProvider` and `OpenAICompatibleProvider` import were added in 02-03. Do NOT add them again — check the current file state.

  Three registration blocks (after graph::memory::semantic, before re-exports):
  ```
  // graph::memory::synthesizer — cron 2AM, batch distillation episodic→semantic+procedural
  const synthesizerWorker = new MemorySynthesizerWorker(pool, llmProvider);
  worker.registerFunction('graph::memory::synthesizer', async (_payload: unknown) => {
    const result = await synthesizerWorker.runSynthesis();
    if (!result.skipped) {
      // Trigger ProceduralMemoryWorker — this is the missing synthesizer→procedural link.
      // TriggerAction.Void() = fire-and-forget; no need to await the procedural write.
      await worker.trigger({
        function_id: 'graph::memory::procedural',
        payload: {
          scope_id: result.scope_id,
          entity_id: randomUUID(),          // new entity per synthesis run
          predecessor_hash: '0'.repeat(64), // ZERO_HASH — first version of synthesis entity
          intent_description: result.intent_description,
          template_graph: result.template_graph,
          nodes: result.nodes,
          edges: result.edges,
        },
        action: TriggerAction.Void(),
      });
    }
    return result;
  });
  worker.registerTrigger(SYNTHESIZER_CRON_TRIGGER);

  // graph::memory::decay — cron 3AM, Ebbinghaus decay scan (G3-6)
  worker.registerFunction('graph::memory::decay', async (_payload: unknown) => {
    await synthesizerWorker.runDecay();
    return { done: true };
  });
  worker.registerTrigger(DECAY_CRON_TRIGGER);

  // graph::memory::ttl — cron 4AM, working_memory 24h TTL purge
  worker.registerFunction('graph::memory::ttl', async (_payload: unknown) => {
    await synthesizerWorker.runTtlPurge();
    return { done: true };
  });
  worker.registerTrigger(TTL_CRON_TRIGGER);
  ```

  The `synthesizerWorker` instance is shared across all three registrations — decay and ttl reuse it.
  </action>

  <verify>
    <automated>npm run typecheck 2>&1 | tail -5</automated>
    <automated>grep -c "graph::memory::synthesizer\|graph::memory::decay\|graph::memory::ttl" packages/workers/src/index.ts</automated>
  </verify>

  <acceptance_criteria>
  - `npm run typecheck` exits 0
  - grep count is 6 (registerFunction + registerTrigger × 3 cron functions)
  - `worker.trigger({ function_id: 'graph::memory::procedural', ..., action: TriggerAction.Void() })` is called when `result.skipped === false`
  - `TriggerAction` is imported from 'iii-sdk'; `randomUUID` is imported from 'crypto'
  - `synthesizerWorker` instantiated exactly once using `llmProvider`
  - `llmProvider` appears exactly once in index.ts (not duplicated from 02-03)
  - All prior registrations (frontier, discover, episodic, semantic) unchanged
  - All three tests from prior plans still pass
  </acceptance_criteria>

  <done>index.ts registers all three cron workers; typecheck passes; no duplication of llmProvider</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iii-cron → MemorySynthesizerWorker | No payload to validate for cron functions; trigger is internal schedule |
| episodic_memory content → LLM | writeGuard applied before LLM input |
| runDecay UPDATE | Purely SQL-driven — affects only rows matching all three WHERE conditions; superseded_by IS NULL guard prevents double-decay |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Denial of Service | runDecay UPDATE on large table | accept | One-time daily cron at 3AM; pg handles large UPDATE atomically; no row-level lock escalation |
| T-02-04-02 | Tampering | runTtlPurge bulk DELETE | accept | WHERE clause is time-bounded; accidental re-run of cron is idempotent (already-deleted rows gone) |
| T-02-04-03 | Information Disclosure | LLM input (combined episodic content) | mitigate | writeGuard applied before LLM call |
| T-02-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
After both tasks:
- `npx vitest run packages/workers/src/memory/synthesizer.worker.test.ts` exits 0, all 6 tests pass
- `npm run typecheck` exits 0
- `grep "graph::memory::decay" packages/workers/src/index.ts` confirms Ebbinghaus decay registered
- `grep "superseded_by = id" packages/workers/src/memory/synthesizer.worker.ts` confirms decay SQL
- All prior workers still registered: grep for frontier, discover, episodic, semantic
</verification>

<success_criteria>
Phase 2, Plan 04 complete when:
1. `packages/workers/src/memory/synthesizer.worker.ts` exports `MemorySynthesizerWorker` and all three cron trigger configs
2. `runDecay()` uses correct Ebbinghaus SQL with `superseded_by = id` guard (G3-6)
3. `runTtlPurge()` deletes working_memory rows older than 24 hours
4. All three functions registered in `packages/workers/src/index.ts`
5. `npm run typecheck` exits 0
</success_criteria>

<output>
Create `.planning/phases/02-memory-retrieval/02-04-SUMMARY.md` when done.
</output>
