---
phase: 01-core-graph-engine
plan: 05
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - packages/workers/package.json
  - packages/workers/src/base/graph-handle.ts
  - packages/workers/src/base/read-only-handle.ts
  - packages/workers/src/base/worker.abstract.ts
  - packages/workers/src/base/tool.interface.ts
  - packages/workers/src/base/lifecycle.ts
  - packages/workers/src/base/subagent.ts
  - src/__tests__/tool-boundary.test.ts
  - src/__tests__/worker-lifecycle.test.ts
autonomous: true
requirements: [REQ-11, REQ-12, REQ-13, REQ-14, REQ-23]
must_haves:
  truths:
    - "Worker holds GraphHandle (with write()); Tool holds ReadOnlyGraphHandle (no write() in interface)"
    - "TypeScript compile fails when a Tool calls ctx.graph.write()"
    - "ReadOnlyGraphHandleImpl.write() throws SecurityException at runtime"
    - "Worker lifecycle is Initializing -> Processing -> Writing -> Terminated with zero persistent writes during Processing"
    - "Subagent branching creates child Scope UUID with spawned_by hyperedge, MAX_CHILD_SCOPE_DEPTH=3, in-process only"
  artifacts:
    - path: "packages/workers/src/base/read-only-handle.ts"
      provides: "ReadOnlyGraphHandle interface + SecurityException runtime guard"
      contains: "SecurityException"
    - path: "packages/workers/src/base/worker.abstract.ts"
      provides: "abstract Worker class with GraphHandle"
      contains: "GraphHandle"
    - path: "packages/workers/src/base/lifecycle.ts"
      provides: "4-phase lifecycle state machine + Knapsack failure bifurcation"
      contains: "Processing"
  key_links:
    - from: "packages/workers/src/base/worker.abstract.ts"
      to: "packages/workers/src/base/graph-handle.ts"
      via: "ctx.graph: GraphHandle"
      pattern: "GraphHandle"
---

<objective>
Build the Worker/Tool ABC framework enforcing the capability boundary: Workers get `GraphHandle` (with `write()`), Tools get `ReadOnlyGraphHandle` (no `write()` in signature → compile error; runtime SecurityException on `any` bypass). Implement the 4-phase Worker lifecycle state machine with Knapsack failure bifurcation, per-tool-result write timing, and Phase 1 subagent scope branching.

Purpose: ADR 35 capability enforcement (REQ-11), ADR 27 lifecycle (REQ-12), ADR 36 write timing (REQ-13), ADR 34 subagent branching (REQ-14), ADR 33 scope-identity orthogonality (REQ-23).
Output: workers package base layer + tool-boundary (compile) and lifecycle (unit) tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-graph-engine/01-RESEARCH.md
@.planning/phases/01-core-graph-engine/01-CONTEXT.md
@docs/ADR_v4.md
@packages/shared/src/constants.ts
@packages/shared/src/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GraphHandle / ReadOnlyGraphHandle ABC + SecurityException (REQ-11, REQ-23)</name>
  <files>packages/workers/package.json, packages/workers/src/base/graph-handle.ts, packages/workers/src/base/read-only-handle.ts, src/__tests__/tool-boundary.test.ts</files>
  <read_first>
    - .planning/phases/01-core-graph-engine/01-RESEARCH.md (Pattern 7 full ABC implementation, GraphHandle vs ReadOnlyGraphHandle, SecurityException runtime guard)
    - .planning/phases/01-core-graph-engine/01-CONTEXT.md (Worker holds GraphHandle, Tool holds ReadOnlyGraphHandle; Scope UUID orthogonality ADR 33)
    - .planning/REQUIREMENTS.md (REQ-11, REQ-23 exact wording)
    - docs/adr/0037-adr35-worker-tool-boundary-enforcement.md
  </read_first>
  <behavior>
    - A Tool whose execute() calls ctx.graph.write() produces a TypeScript compile error (tsc --noEmit fails on a fixture)
    - ReadOnlyGraphHandleImpl.write() throws SecurityException at runtime
    - GraphHandle.write() delegates to occWrite and returns WriteResult
    - Scope UUID passed to a handle is never mutated by context-size operations (orthogonality assertion)
  </behavior>
  <action>
    Create `packages/workers/package.json` (name `@graph/workers`, deps `iii-sdk`, `pg`, `zod`, `@graph/shared`). Per RESEARCH.md Pattern 7: `graph-handle.ts` exports `interface GraphHandle { write(event: GraphWriteEvent): Promise<WriteResult>; query<T>(sql, params?): Promise<T[]> }` and a class `GraphHandleImpl` whose `write()` calls `occWrite` from `@graph/shared`. `read-only-handle.ts` exports `interface ReadOnlyGraphHandle { query<T>(sql, params?): Promise<T[]> }` (write ABSENT from interface), class `ReadOnlyGraphHandleImpl implements ReadOnlyGraphHandle` with a `query` method and a non-interface `write(_e: unknown): never { throw new SecurityException(...) }` runtime guard, and `class SecurityException extends Error`. Document in a comment that the Scope UUID held by a handle is the business-task identity and is NEVER rotated on context overflow (ADR 33 / REQ-23). Write `src/__tests__/tool-boundary.test.ts`: (a) a `// @ts-expect-error` fixture proving `roHandle.write(...)` is a compile error, (b) runtime test that `new ReadOnlyGraphHandleImpl(...).write({})` throws `SecurityException`.
  </action>
  <verify>
    <automated>cd D:/Repo/graph-enginerring && npm run test:unit -- tool-boundary && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `read-only-handle.ts` interface `ReadOnlyGraphHandle` does NOT declare `write`
    - `read-only-handle.ts` exports `SecurityException` and `ReadOnlyGraphHandleImpl.write` throws it
    - `tool-boundary.test.ts` contains a `@ts-expect-error` line on a `roHandle.write` call
    - `tool-boundary.test.ts` asserts `write` throws `SecurityException` at runtime
    - `npm run test:unit -- tool-boundary` exits 0 and `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>ABC boundary compiles; Tool write() is a compile error; runtime guard throws SecurityException.</done>
</task>

<task type="auto">
  <name>Task 2: Worker abstract class + Tool interface + 4-phase lifecycle (REQ-12, REQ-13)</name>
  <files>packages/workers/src/base/worker.abstract.ts, packages/workers/src/base/tool.interface.ts, packages/workers/src/base/lifecycle.ts, src/__tests__/worker-lifecycle.test.ts</files>
  <read_first>
    - .planning/phases/01-core-graph-engine/01-RESEARCH.md (Pattern 7 Worker abstract class + Tool interface; Anti-pattern no writes during Processing)
    - .planning/phases/01-core-graph-engine/01-CONTEXT.md (Worker lifecycle Initializing->Processing->Writing->Terminated; Knapsack failure bifurcation; per-tool-result write ADR 36)
    - .planning/REQUIREMENTS.md (REQ-12, REQ-13)
    - docs/adr/0029-adr27-worker-lifecycle-state-machine.md; docs/adr/0038-adr36-knowledge-entity-write-timing.md
  </read_first>
  <action>
    Create `worker.abstract.ts`: `abstract class Worker` with abstract methods `onScheduled`, `onRunning`, `onCompleted`, `onFailed`, `onConflicted` taking `WorkerExecutionContext { scopeId, entityId, currentVersionHash, graph: GraphHandle, input }`. Create `tool.interface.ts`: `interface Tool<TInput extends z.ZodType, TOutput extends z.ZodType>` with `inputSchema`, `outputSchema`, `execute(input, ctx: ToolExecutionContext)` where `ToolExecutionContext { graph: ReadOnlyGraphHandle }`. Create `lifecycle.ts`: enum/union `LifecyclePhase = 'Initializing' | 'Processing' | 'Writing' | 'Terminated'` and a `runLifecycle(worker, ctx)` driver enforcing the rule that `graph.write()` is rejected during Processing (throw if write attempted while phase==='Processing' — LLM results stay in memory until Writing). Implement Knapsack failure bifurcation helper `classifyKnapsackFailure(err): 'size' | 'load'` and a re-queue counter capped at 3 (then escalate to OOM chain). Implement per-tool-result write timing: helper `writeToolResult(graph, result)` using the DO NOTHING idempotent path immediately after a state-changing tool returns (read-only results may be skipped). Write `src/__tests__/worker-lifecycle.test.ts` (unit, mock GraphHandle): assert a write attempt during Processing is rejected, and a write during Writing succeeds; assert load-cause failure re-queues up to 3 then escalates.
  </action>
  <verify>
    <automated>cd D:/Repo/graph-enginerring && npm run test:unit -- worker-lifecycle && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `worker.abstract.ts` defines abstract `Worker` with `graph: GraphHandle` in context
    - `tool.interface.ts` defines `Tool` with `graph: ReadOnlyGraphHandle` in context
    - `lifecycle.ts` rejects `write()` during the `Processing` phase
    - `lifecycle.ts` caps load-cause re-queue at 3 before escalating
    - `worker-lifecycle.test.ts` asserts write-during-Processing is rejected
    - `npm run test:unit -- worker-lifecycle` exits 0 and `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Worker/Tool ABCs + 4-phase lifecycle with no-write-during-Processing rule, Knapsack bifurcation, and per-tool-result write timing.</done>
</task>

<task type="auto">
  <name>Task 3: Subagent scope branching Phase 1 (REQ-14)</name>
  <files>packages/workers/src/base/subagent.ts</files>
  <read_first>
    - .planning/phases/01-core-graph-engine/01-CONTEXT.md (Subagent branching Phase 1: in-process, child Scope UUID, spawned_by hyperedge, MAX_CHILD_SCOPE_DEPTH=3, env GRAPH_AGENT_CHILD_SCOPE + payload spawned_by_scope)
    - .planning/REQUIREMENTS.md (REQ-14)
    - packages/shared/src/constants.ts (MAX_CHILD_SCOPE_DEPTH)
    - docs/adr/0036-adr34-subagent-scope-branch-model.md
  </read_first>
  <action>
    Create `subagent.ts` exporting `async function spawnChildScope(graph: GraphHandle, parentScopeId: string, parentDepth: number, input: unknown): Promise<{ childScopeId }>`. Guards: throw if `parentDepth + 1 > MAX_CHILD_SCOPE_DEPTH` (=3); require `process.env.GRAPH_AGENT_CHILD_SCOPE` to be set (in-process only Phase 1). Generate a child Scope UUID, write a `spawned_by` hyperedge as a graph event payload carrying `spawned_by_scope = parentScopeId` (tuple shape `(parent_scope_id, child_scope_id, 'scope_spawned', version_hash, timestamp)` per ADR 34). Phase 1 executes the child in-process only — add a comment that distributed/forked execution is Phase 4. Do NOT call the Control Plane nesting protocol here (that is Gateway/Control-Plane responsibility); this records the lineage edge.
  </action>
  <verify>
    <automated>cd D:/Repo/graph-enginerring && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `subagent.ts` throws when `parentDepth + 1 > MAX_CHILD_SCOPE_DEPTH`
    - `subagent.ts` references `process.env.GRAPH_AGENT_CHILD_SCOPE` and payload field `spawned_by_scope`
    - `subagent.ts` writes a `scope_spawned` / `spawned_by` hyperedge
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Subagent branching enforces MAX_DEPTH=3, env guard, in-process-only, and appends spawned_by hyperedge.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0; `npm run test:unit -- tool-boundary worker-lifecycle` green
- Tool write() is compile error + runtime SecurityException
- No persistent write during Processing phase
</verification>

<success_criteria>
- ABC capability boundary enforced at compile + runtime (REQ-11)
- 4-phase lifecycle with Knapsack bifurcation + per-tool-result writes (REQ-12, REQ-13)
- Subagent branching MAX_DEPTH=3 in-process (REQ-14); Scope UUID orthogonal to context (REQ-23)
</success_criteria>

<output>
Create `.planning/phases/01-core-graph-engine/01-05-SUMMARY.md` when done
</output>
