---
phase: 01-core-graph-engine
plan: 09
type: execute
wave: 5
depends_on: ["01-05", "01-06", "01-08", "01-10"]
files_modified:
  - packages/workers/src/llm/provider.interface.ts
  - packages/workers/src/llm/openai-compatible.provider.ts
  - packages/workers/src/concrete/context-assembly.worker.ts
  - packages/workers/src/concrete/conflict-resolver.worker.ts
  - packages/workers/src/index.ts
  - src/__tests__/llm-provider.test.ts
autonomous: true
requirements: [REQ-21]
must_haves:
  truths:
    - "LLMProvider and EmbeddingProvider interfaces defined in the iii-engine/worker layer; Workers call interface and never hold credentials"
    - "One OpenAI-compatible provider implemented hitting /v1/chat/completions and /v1/embeddings"
    - "Credentials sourced only from iii-config.yaml env interpolation, never hardcoded in Workers"
    - "Embedding calls are not counted against the Worker token budget"
    - "ContextAssemblyWorker registered; ConflictResolverWorker is a Phase 1 stub (OCC hard-stop is enough)"
  artifacts:
    - path: "packages/workers/src/llm/provider.interface.ts"
      provides: "LLMProvider + EmbeddingProvider interfaces"
      contains: "EmbeddingProvider"
    - path: "packages/workers/src/llm/openai-compatible.provider.ts"
      provides: "OpenAI-compatible REST provider"
      contains: "/v1/chat/completions"
  key_links:
    - from: "packages/workers/src/concrete/context-assembly.worker.ts"
      to: "packages/workers/src/context/assemble.ts"
      via: "assembleContext"
      pattern: "assembleContext"
    - from: "packages/workers/src/index.ts"
      to: "packages/workers/src/scheduler/frontier.worker.ts"
      via: "FrontierSchedulerWorker + FRONTIER_TRIGGER_CONFIG registration (sole entry point)"
      pattern: "FRONTIER_TRIGGER_CONFIG"
    - from: "packages/workers/src/index.ts"
      to: "packages/workers/src/patterns/discover.worker.ts"
      via: "PatternDiscoveryWorker + PATTERN_DISCOVERY_CRON_TRIGGER registration (sole entry point)"
      pattern: "PATTERN_DISCOVERY_CRON_TRIGGER"
---

<objective>
Define the LLMProvider / EmbeddingProvider interfaces and implement exactly one OpenAI-compatible REST provider (ollama / llama.cpp / lmstudio / OpenAI). Wire the concrete ContextAssemblyWorker (uses Plan 08 assembler) and a Phase 1 ConflictResolverWorker stub (OCC hard-stop suffices). Register all workers via iii-sdk.

Purpose: REQ-21 — credentials live only in iii-config.yaml; Workers call the interface. Every LLM call site is annotated with its justifying ADR.
Output: provider interface + OpenAI-compatible impl + 2 concrete workers + registration; provider unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-graph-engine/01-RESEARCH.md
@.planning/phases/01-core-graph-engine/01-CONTEXT.md
@docs/ADR_v4.md
@.harness/research/iii-engine.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LLMProvider/EmbeddingProvider interfaces + OpenAI-compatible provider (REQ-21)</name>
  <files>packages/workers/src/llm/provider.interface.ts, packages/workers/src/llm/openai-compatible.provider.ts, src/__tests__/llm-provider.test.ts</files>
  <read_first>
    - .planning/phases/01-core-graph-engine/01-RESEARCH.md (Pattern 8 iii-config.yaml llm + embedding blocks; ADR 22 credentials in config only)
    - .planning/phases/01-core-graph-engine/01-CONTEXT.md (LLM Provider Abstraction: interfaces in iii-engine layer, one OpenAI-compatible provider, embedding calls not counted against budget, every LLM call annotated with ADR)
    - .planning/REQUIREMENTS.md (REQ-21)
    - docs/adr/0023-adr22-llm-provider-abstraction.md
  </read_first>
  <behavior>
    - OpenAICompatibleProvider.chat(messages) POSTs to {base_url}/v1/chat/completions and returns the assistant content
    - OpenAICompatibleProvider.embed(text) POSTs to {base_url}/v1/embeddings and returns number[]
    - provider reads base_url/model/api_key from injected config (env-sourced), never from hardcoded literals
    - embed() result is tagged so callers know it is not counted against the token budget
  </behavior>
  <action>
    Create `provider.interface.ts` exporting `interface LLMProvider { chat(messages: ChatMessage[], opts?): Promise<string> }` and `interface EmbeddingProvider { embed(text: string): Promise<number[]> }`. Create `openai-compatible.provider.ts` exporting `class OpenAICompatibleProvider implements LLMProvider, EmbeddingProvider`, constructed with `{ baseUrl, model, apiKey }` injected from config (sourced from iii-config.yaml env interpolation — NEVER read credentials inside a Worker). `chat()` POSTs `{base_url}/v1/chat/completions`; `embed()` POSTs `{base_url}/v1/embeddings`. Add a comment at each network call: `// LLM CALL — justified by ADR 22`. Mark embed() returns as not-budget-counted (comment + a returned `{ vector, countedAgainstBudget: false }` shape or doc). Write `src/__tests__/llm-provider.test.ts` mocking `fetch`: assert chat() POSTs to the chat-completions path and embed() POSTs to the embeddings path; assert no credential literal appears in the provider source (test reads the file and asserts no `sk-` / api-key literal).
  </action>
  <verify>
    <automated>cd D:/Repo/graph-enginerring && npm run test:unit -- llm-provider && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `provider.interface.ts` exports `LLMProvider` and `EmbeddingProvider`
    - `openai-compatible.provider.ts` contains `/v1/chat/completions` and `/v1/embeddings`
    - provider takes `apiKey` via constructor injection (no hardcoded credential literal)
    - each network call site has an `LLM CALL` ADR-22 annotation comment
    - `npm run test:unit -- llm-provider` exits 0 and `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Provider interfaces + one OpenAI-compatible REST impl with injected credentials and annotated call sites.</done>
</task>

<task type="auto">
  <name>Task 2: ContextAssemblyWorker + ConflictResolverWorker stub + registration</name>
  <files>packages/workers/src/concrete/context-assembly.worker.ts, packages/workers/src/concrete/conflict-resolver.worker.ts, packages/workers/src/index.ts</files>
  <read_first>
    - .planning/phases/01-core-graph-engine/01-CONTEXT.md (ConflictResolverWorker LLM-assisted merge OUT OF SCOPE Phase 1, OCC hard-stop enough; per-tool-result write discipline)
    - .planning/phases/01-core-graph-engine/01-RESEARCH.md (Pattern 1 registerWorker/registerFunction; Pattern 7 registration)
    - packages/workers/src/base/worker.abstract.ts (Worker abstract class — Plan 05)
    - packages/workers/src/context/assemble.ts (assembleContext — Plan 08)
  </read_first>
  <action>
    Create `context-assembly.worker.ts`: `class ContextAssemblyWorker extends Worker` whose `onRunning` calls `assembleContext(...)` from `../context/assemble` and writes the projection (via GraphHandle during the Writing phase only). Create `conflict-resolver.worker.ts`: Phase 1 STUB `class ConflictResolverWorker extends Worker` — add a comment that LLM-assisted merge is Phase 2 and that OCC Writable CTE causal inversion (Plan 03) already resolves conflicts deterministically in Phase 1; `onConflicted` logs and no-ops (no LLM call). Create/extend `packages/workers/src/index.ts` as the SINGLE boot entry point for ALL worker registrations: `registerWorker(III_URL, { workerName: 'graph-workers' })`, then register all four workers: (1) `registerFunction('graph::context-assembly', ...)`, (2) `registerFunction('graph::conflict-resolver', ...)`, (3) import `FrontierSchedulerWorker, FRONTIER_TRIGGER_CONFIG` from `../scheduler/frontier.worker` and call `registerFunction('graph::scheduler::frontier', ...)` + `registerTrigger(FRONTIER_TRIGGER_CONFIG)`, (4) import `PatternDiscoveryWorker, PATTERN_DISCOVERY_CRON_TRIGGER` from `../patterns/discover.worker` and call `registerFunction('graph::patterns::discover', ...)` + `registerTrigger(PATTERN_DISCOVERY_CRON_TRIGGER)`. This is the only file that calls registerFunction/registerTrigger — Plans 06 and 10 export Workers and trigger config constants but do NOT self-register. Construct the OpenAICompatibleProvider from config and inject into workers — no Worker reads credentials directly.
  </action>
  <verify>
    <automated>cd D:/Repo/graph-enginerring && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `context-assembly.worker.ts` extends `Worker` and calls `assembleContext`
    - `conflict-resolver.worker.ts` is a stub with NO LLM call and a comment citing OCC hard-stop / Phase 2 deferral
    - `index.ts` calls `registerWorker` and `registerFunction('graph::context-assembly'`
    - no concrete Worker contains a credential literal (credentials injected from config)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>ContextAssemblyWorker wired to assembler; ConflictResolverWorker Phase 1 stub; all workers registered via iii-sdk.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0; `npm run test:unit -- llm-provider` green
- Credentials only via injected config; LLM call sites annotated with ADR 22
- ConflictResolver is a no-LLM stub (OCC hard-stop suffices)
</verification>

<success_criteria>
- LLMProvider/EmbeddingProvider + one OpenAI-compatible provider, credentials in config only (REQ-21)
- ContextAssemblyWorker + ConflictResolver stub registered via iii-sdk
</success_criteria>

<output>
Create `.planning/phases/01-core-graph-engine/01-09-SUMMARY.md` when done
</output>
