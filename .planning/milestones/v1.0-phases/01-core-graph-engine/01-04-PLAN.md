---
phase: 01-core-graph-engine
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - packages/control-plane/package.json
  - packages/control-plane/src/db/ddl-pool.ts
  - packages/control-plane/src/db/read-pool.ts
  - packages/control-plane/src/nesting.ts
  - packages/control-plane/src/hwm.ts
  - packages/control-plane/src/pulse-fetch.ts
  - packages/control-plane/src/watchdog.ts
  - packages/control-plane/src/index.ts
  - src/__tests__/watchdog.test.ts
autonomous: true
requirements: [REQ-06, REQ-07, REQ-08, REQ-09]
must_haves:
  truths:
    - "Control Plane holds two separate DB pools: DDL-exclusive and event-read/LISTEN"
    - "3-phase nesting protocol runs in a single DDL transaction: create partition+constraints+indexes, insert scope_lineage, insert plan_created"
    - "pg-listen Pulse-Fetch bridge advances HWM in bus_state then calls iii.trigger() — boot order LISTEN before HWM read"
    - "Convergence Watchdog is the sole scope_closed emitter within the Control Plane Daemon via 3-tier defense; the Gateway (ADR 24) separately holds infra-write rights for scope_closed via its inline Watchdog SQL path"
  artifacts:
    - path: "packages/control-plane/src/nesting.ts"
      provides: "3-phase DDL nesting protocol in single transaction"
      contains: "PARTITION OF execution_event_log"
    - path: "packages/control-plane/src/pulse-fetch.ts"
      provides: "pg-listen subscriber -> HWM advance -> iii.trigger bridge"
      contains: "notifications.on"
    - path: "packages/control-plane/src/watchdog.ts"
      provides: "3-tier Convergence Watchdog, sole scope_closed emitter within Control Plane Daemon"
      contains: "scope_closed"
  key_links:
    - from: "packages/control-plane/src/pulse-fetch.ts"
      to: "bus_state"
      via: "HWM advance UPDATE before iii.trigger"
      pattern: "last_processed_event_id"
---

<objective>
Build the Control Plane Daemon: two DB connection pools (DDL exclusive + event read/LISTEN), the 3-phase scope nesting protocol, the pg-listen Pulse-Fetch bridge with HWM tracking, and the embedded Topological Convergence Watchdog (sole scope_closed emitter within the Control Plane Daemon; Gateway separately holds ADR-24 infra-write rights).

Purpose: The Control Plane is the orchestration heart (REQ-06, REQ-07, REQ-08, REQ-09). It bridges PostgreSQL LISTEN/NOTIFY to iii.trigger(), owns all DDL, and detects scope convergence.
Output: Control Plane package with nesting, pulse-fetch, watchdog, OOM degradation; watchdog unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-graph-engine/01-RESEARCH.md
@.planning/phases/01-core-graph-engine/01-CONTEXT.md
@docs/ADR_v4.md
@.harness/research/iii-engine.md
@packages/shared/src/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB pools + 3-phase nesting protocol (REQ-06, REQ-07)</name>
  <files>packages/control-plane/package.json, packages/control-plane/src/db/ddl-pool.ts, packages/control-plane/src/db/read-pool.ts, packages/control-plane/src/nesting.ts</files>
  <read_first>
    - .planning/phases/01-core-graph-engine/01-RESEARCH.md (Scope Partition DDL example, Architectural Responsibility Map DDL row, Pitfall 5 per-partition constraint)
    - .planning/phases/01-core-graph-engine/01-CONTEXT.md (Control Plane: two DB pools; 3-phase nesting protocol ADR 05)
    - migrations/002-event-log.sql, migrations/005-scope-lineage.sql (column names)
    - docs/ADR_v4.md (ADR 05, ADR 04)
  </read_first>
  <action>
    Create `packages/control-plane/package.json` (name `@graph/control-plane`, deps `pg`, `pg-listen`, `iii-sdk`, `@graph/shared`). Create `db/ddl-pool.ts` exporting `ddlPool` — a `pg.Pool` with `max: 2` (exclusive DDL connection, must not be shared). Create `db/read-pool.ts` exporting `readPool` — separate `pg.Pool` for point-query reads + HWM. Create `nesting.ts` exporting `async function nestScope(intent: string, parentScopeId?: string, depth = 0): Promise<{ scopeId, planHash }>` executing the 3-phase protocol in a SINGLE transaction on `ddlPool` (BEGIN/COMMIT): Phase 1 — generate scope UUID, `CREATE TABLE execution_event_log_scope_{nodash} PARTITION OF execution_event_log FOR VALUES IN ('{uuid}')`, then `ALTER TABLE ... ADD CONSTRAINT uk_scope_occ_{nodash} UNIQUE (predecessor_hash, scope_id)`, the idempotency `UNIQUE (scope_id, entity_id, version_hash)`, and the pending-lookup partial index `idx_scope_{nodash}_pending_lookup ON (...)(scope_id, status, event_id ASC) WHERE status IN ('pending_scheduling','pending_dispatch')`; Phase 2 — `INSERT INTO scope_lineage (scope_id, parent_scope_id, depth, intent)` enforcing `depth <= MAX_CHILD_SCOPE_DEPTH`; Phase 3 — INSERT first `plan_created` event with `predecessor_hash = ZERO_HASH` and version_hash via pgcrypto digest. On any failure ROLLBACK the whole transaction. Validate identifier interpolation (UUID hex only, no SQL injection) before building DDL strings.
  </action>
  <verify>
    <automated>cd D:/Repo/graph-enginerring && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `db/ddl-pool.ts` exports a pool with `max: 2` distinct from `read-pool.ts`
    - `nesting.ts` contains `PARTITION OF execution_event_log` and `UNIQUE (predecessor_hash, scope_id)` and `ZERO_HASH`
    - `nesting.ts` wraps all 3 phases in a single BEGIN/COMMIT transaction with ROLLBACK on error
    - `nesting.ts` validates scope UUID is hex before string interpolation into DDL
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Two pools created; nesting protocol runs all 3 phases atomically and creates per-partition OCC + idempotency + lookup constraints.</done>
</task>

<task type="auto">
  <name>Task 2: HWM tracking + pg-listen Pulse-Fetch bridge (REQ-06)</name>
  <files>packages/control-plane/src/hwm.ts, packages/control-plane/src/pulse-fetch.ts</files>
  <read_first>
    - .planning/phases/01-core-graph-engine/01-RESEARCH.md (Pattern 2 pg-listen exact API, Pitfall 2 notifications.on, Pitfall 3 boot order LISTEN before HWM read, Pattern 1 iii.trigger, Open Question 3 trigger semantics)
    - .planning/phases/01-core-graph-engine/01-CONTEXT.md (Pulse-Fetch Bridge: pg-listen callback -> HWM advance -> iii.trigger; LISTEN/NOTIFY carries no data)
    - .harness/research/iii-engine.md (registerWorker / trigger API surface)
    - packages/control-plane/src/db/read-pool.ts
  </read_first>
  <action>
    Create `hwm.ts` exporting `async function advanceHwm(pool, workerId, eventId)` running `UPDATE bus_state SET last_processed_event_id = $1 WHERE worker_id = $2 AND last_processed_event_id < $1` and `async function readHwm(pool, workerId): Promise<number>`. Create `pulse-fetch.ts` exporting `async function startPulseFetch(deps: { iiiWorker })`: construct `createSubscriber({ connectionString: process.env.DATABASE_URL })` from `pg-listen`. Boot order per Pitfall 3 MUST be: (1) `await subscriber.connect()`, (2) `await subscriber.listenTo('graph_event_ready')`, (3) read HWM, (4) replay events with `id > hwm` from execution_event_log. Register handler via `subscriber.notifications.on('graph_event_ready', cb)` (NOT `client.on`) — the cb parses `{ id }` from the ≤64B payload, point-queries the event from readPool, calls `advanceHwm`, then `await iiiWorker.trigger({ function_id: ` + "`worker::${event.event_type}`" + `, payload: event })`. Attach `subscriber.events.on('error', ...)` that logs but does NOT exit (pg-listen auto-reconnects). Use the iii-sdk `registerWorker` returned object for `.trigger()`.
  </action>
  <verify>
    <automated>cd D:/Repo/graph-enginerring && node -e "const s=require('fs').readFileSync('packages/control-plane/src/pulse-fetch.ts','utf8');process.exit(/notifications\.on/.test(s) && /listenTo/.test(s) && /trigger/.test(s) && !/client\.on\('notification'/.test(s) ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `pulse-fetch.ts` uses `subscriber.notifications.on(` and `subscriber.listenTo(` and does NOT use `client.on('notification'`
    - `pulse-fetch.ts` calls `listenTo` BEFORE reading HWM (boot order)
    - `pulse-fetch.ts` calls `advanceHwm` before `iiiWorker.trigger`
    - `hwm.ts` UPDATE includes guard `last_processed_event_id < $1`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Pulse-Fetch bridge subscribes with correct API + boot order, advances HWM, triggers iii workers.</done>
</task>

<task type="auto">
  <name>Task 3: Convergence Watchdog (3-tier) + OOM degradation + boot (REQ-08, REQ-09)</name>
  <files>packages/control-plane/src/watchdog.ts, packages/control-plane/src/index.ts, src/__tests__/watchdog.test.ts</files>
  <read_first>
    - .planning/phases/01-core-graph-engine/01-RESEARCH.md (Convergence Watchdog SQL example, Architectural Responsibility Map convergence row)
    - .planning/phases/01-core-graph-engine/01-CONTEXT.md (Convergence Watchdog 3-tier defense embedded in Control Plane sole scope_closed emitter; Context OOM 3-tier chain ADR 13 supplement)
    - .planning/REQUIREMENTS.md (REQ-08, REQ-09 exact wording)
    - docs/ADR_v4.md (ADR 19, ADR 28); docs/adr/0024-adr13-supplement-context-oom-degradation.md
  </read_first>
  <action>
    Create `watchdog.ts` exporting class `ScopeConvergenceTracker` with 3-tier defense: Tier 1 in-memory atomic counters per scope (`pendingTasks`, `openConflicts` maps) with `increment/decrement` methods; Tier 2 a per-scope conflict lock (`Set<scopeId>` guarding concurrent close checks); Tier 3 the DB terminal SQL from RESEARCH.md (`SELECT NOT EXISTS(... status NOT IN terminated/archived ...) AS is_converged, NOT EXISTS(... conflict_detected AND status != resolved ...) AS no_open_conflicts`). Method `async checkAndClose(scopeId)`: only when Tier1 counters are zero AND Tier2 lock acquired, run Tier3 SQL; if `is_converged AND no_open_conflicts`, INSERT a `scope_closed` event (this class is the ONLY code path that emits scope_closed). Add `async handleContextOom(scopeId, tier)` implementing the 3-tier chain: Tier 1 = LLM distill of N_root (annotate the call site with comment `// LLM CALL — justified by ADR 13 supplement`), Tier 2 = tail-truncate N_current to last 2000 tokens, Tier 3 = direct-write `context_oom_throttled` and mark scope Suspended. Create `index.ts` boot entry: register iii worker `control-plane`, start pulse-fetch, instantiate watchdog. Write `src/__tests__/watchdog.test.ts` (unit, mock pool): assert checkAndClose does NOT emit scope_closed when pendingTasks>0, and DOES emit when counters zero + SQL returns converged.
  </action>
  <verify>
    <automated>cd D:/Repo/graph-enginerring && npm run test:unit -- watchdog && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `watchdog.ts` contains `ScopeConvergenceTracker` with three tiers (in-memory counters, conflict lock Set, DB SQL)
    - `watchdog.ts` is the only file inserting `scope_closed` within the control-plane package (grep scoped to packages/control-plane/ — the Gateway separately holds ADR-24 infra-write rights for scope_closed via its inline Watchdog SQL path)
    - `watchdog.ts` OOM Tier 1 has an explicit `LLM CALL` annotation comment citing ADR 13
    - `watchdog.ts` OOM Tier 3 writes `context_oom_throttled`
    - `src/__tests__/watchdog.test.ts` asserts no scope_closed when pendingTasks>0
    - `npm run test:unit -- watchdog` exits 0 and `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Watchdog enforces 3-tier convergence and is the sole scope_closed emitter within Control Plane Daemon; OOM chain implemented with annotated LLM call; boot wires pulse-fetch + watchdog.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0; `npm run test:unit -- watchdog` green
- Nesting runs all 3 phases atomically; pg-listen uses correct API + boot order
- scope_closed emitted only by the Watchdog within Control Plane Daemon (Gateway inline Watchdog path is a separate ADR-24-granted right)
</verification>

<success_criteria>
- Two-pool Control Plane with atomic 3-phase nesting (REQ-06, REQ-07)
- Pulse-Fetch HWM bridge to iii.trigger
- 3-tier Convergence Watchdog sole scope_closed emitter within Control Plane Daemon (REQ-08); Gateway separately holds ADR-24 infra-write rights; OOM 3-tier chain (REQ-09)
</success_criteria>

<output>
Create `.planning/phases/01-core-graph-engine/01-04-SUMMARY.md` when done
</output>
