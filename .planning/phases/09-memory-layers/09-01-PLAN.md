---
phase: 09-memory-layers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/012-memory-embedding.sql
  - packages/workers/src/base/memory-repository.ts
  - packages/workers/src/base/trail-reader.ts
  - packages/workers/src/base/worker.abstract.ts
autonomous: true
requirements:
  - D-02
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
  - D-11
  - D-13

must_haves:
  truths:
    - "episodic_memory rows written by TPW carry an embedding vector(1536) column"
    - "procedural_memory anti-pattern rows are indexed by a dedicated HNSW partial index"
    - "insertSemanticFact returns a suggestedMerge hint when cosine similarity exceeds 0.89"
    - "supersede() writes superseded_by; migration-008 trigger stamps valid_until automatically"
    - "getScopeEvents returns all execution_event_log rows for a scope in insert order"
    - "Worker.shouldReflect() is a public method returning true by default"
  artifacts:
    - path: "migrations/012-memory-embedding.sql"
      provides: "episodic embedding column, episodic HNSW index, negative procedural HNSW index"
      contains: "idx_episodic_memory_embedding_hnsw"
    - path: "packages/workers/src/base/memory-repository.ts"
      provides: "appendEpisodicSummary, updated insertSemanticFact, supersede, isAntiPattern"
      exports: ["EpisodicRepository", "SemanticRepository", "MemoryRepository", "PoolMemoryRepository", "StubMemoryRepository"]
    - path: "packages/workers/src/base/trail-reader.ts"
      provides: "getScopeEvents"
      exports: ["TrailReader", "PoolTrailReader", "StubTrailReader"]
    - path: "packages/workers/src/base/worker.abstract.ts"
      provides: "shouldReflect() public method"
      exports: ["Worker", "PipelineContext", "WorkerExecutionContext"]
  key_links:
    - from: "packages/workers/src/memory/template-proposal.worker.ts (Plan 02)"
      to: "appendEpisodicSummary in PoolMemoryRepository"
      via: "memory.appendEpisodicSummary(scopeId, entityId, intentSummary, outcomeSummary, embeddingLiteral)"
    - from: "packages/workers/src/memory/semantic.worker.ts (Plan 03)"
      to: "insertSemanticFact in PoolMemoryRepository"
      via: "returns { id, suggestedMerge } — caller calls supersede(suggestedMerge.id, newId) if non-null"
    - from: "packages/workers/src/context/assemble.ts (Plan 04)"
      to: "Worker.shouldReflect()"
      via: "worker.shouldReflect() call in runContextAssemblyPipeline cold_start branch"
---

<objective>
Lay the foundation seams that all Phase 9 workers depend on: the DB migration that adds
the episodic embedding column and negative procedural HNSW index, the MemoryRepository
interface extensions for episodic summary writes, semantic supersession, and anti-pattern
writes, the TrailReader.getScopeEvents() method for full-DAG reads, and
Worker.shouldReflect() for the Reflection Track opt-out mechanism.

Purpose: Downstream plans (02, 03, 04) cannot compile or test without these contracts in place.
Output: Migration file + updated base interfaces + stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-memory-layers/09-CONTEXT.md
@.harness/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migration 012 — episodic embedding column + negative procedural HNSW index</name>
  <files>migrations/012-memory-embedding.sql</files>

  <read_first>
    migrations/003-memory-tables.sql — episodic_memory and procedural_memory table definitions
    migrations/006-memory-extensions.sql — existing HNSW index on semantic_memory, existing is_anti_pattern note
    migrations/008-semantic-memory-validity.sql — idempotent DDL pattern to follow
    docs/adr/0021-adr20-supplement-hybrid-retrieval-bm25-rrf.md — HNSW spec: m=16, ef_construction=64, vector_cosine_ops
  </read_first>

  <action>
    Create migrations/012-memory-embedding.sql. Header comment: "Migration 012: episodic embedding
    column + negative procedural HNSW index. Enables Phase 09 TemplateProposalWorker (D-04) and
    dual HNSW anti-pattern retrieval (D-13)."

    Section 1 — episodic_memory embedding (D-04):
    ALTER TABLE episodic_memory ADD COLUMN IF NOT EXISTS embedding vector(1536).
    CREATE INDEX IF NOT EXISTS idx_episodic_memory_embedding_hnsw ON episodic_memory
    USING hnsw (embedding vector_cosine_ops) WITH (m = 16, ef_construction = 64)
    WHERE embedding IS NOT NULL.

    Section 2 — procedural_memory negative HNSW (D-13):
    CREATE INDEX IF NOT EXISTS idx_procedural_memory_topology_hnsw_negative ON procedural_memory
    USING hnsw (topology_embedding vector_cosine_ops) WITH (m = 16, ef_construction = 64)
    WHERE is_anti_pattern = TRUE.

    All DDL uses IF NOT EXISTS — idempotent, safe to re-run. No data alteration. Footer comment
    referencing D-04 and D-13.
  </action>

  <acceptance_criteria>
    - File migrations/012-memory-embedding.sql exists
    - Contains exactly the string: idx_episodic_memory_embedding_hnsw
    - Contains exactly the string: idx_procedural_memory_topology_hnsw_negative
    - Contains: ADD COLUMN IF NOT EXISTS embedding vector(1536)
    - Contains: WHERE is_anti_pattern = TRUE
    - Contains: WHERE embedding IS NOT NULL
    - All CREATE INDEX statements include IF NOT EXISTS
  </acceptance_criteria>

  <verify>
    <automated>grep -c "IF NOT EXISTS" migrations/012-memory-embedding.sql</automated>
  </verify>
  <done>
    Migration file created with episodic embedding column + HNSW index and negative procedural
    HNSW index, all statements idempotent.
  </done>
</task>

<task type="auto">
  <name>Task 2: MemoryRepository seam extensions — appendEpisodicSummary, insertSemanticFact v2, supersede, isAntiPattern</name>
  <files>packages/workers/src/base/memory-repository.ts</files>

  <read_first>
    packages/workers/src/base/memory-repository.ts — full file; extract all existing interfaces,
    PoolMemoryRepository SQL patterns, StubMemoryRepository call-tracking structure
    migrations/008-semantic-memory-validity.sql — trigger that fires when superseded_by is set;
    application code only needs to write superseded_by, not valid_until
    docs/adr/0021-adr20-supplement-hybrid-retrieval-bm25-rrf.md — cosine similarity threshold
    context for the 0.89 cutoff used in the suggestedMerge hint query
  </read_first>

  <action>
    Make four targeted changes to packages/workers/src/base/memory-repository.ts.

    CHANGE 1 — EpisodicRepository interface: add method signature
    appendEpisodicSummary(scopeId: string, entityId: string, intentSummary: string,
    outcomeSummary: string, embeddingLiteral: string): Promise<void>
    Place after the existing appendEpisodicTrace signature.

    CHANGE 2 — SemanticRepository interface: replace insertSemanticFact signature.
    Old: insertSemanticFact(scopeId: string, content: string): Promise<void>
    New: insertSemanticFact(scopeId: string, content: string, embedding: number[]): Promise<{ id: string; suggestedMerge: { id: string; content: string } | null }>
    Also add: supersede(oldId: string, newId: string): Promise<void>

    CHANGE 3 — ProceduralTemplateParams: add optional field isAntiPattern?: boolean after
    the intentEmbeddingLiteral field.

    CHANGE 4 — PoolMemoryRepository implementations:

    appendEpisodicSummary: INSERT INTO episodic_memory
    (scope_id, entity_id, content, intent_summary, outcome_summary, embedding, created_at)
    VALUES ($1, $2, $3, $4, $5, $6::vector, NOW())
    where $3 = intentSummary + '\n' + outcomeSummary (for ts_doc BM25 compatibility since
    ts_doc GENERATED column uses content), $4 = intentSummary, $5 = outcomeSummary,
    $6 = embeddingLiteral passed as-is (caller formats as '[v1,v2,...]').

    insertSemanticFact: Writable CTE pattern —
    WITH inserted AS (INSERT INTO semantic_memory(scope_id, content, embedding, valid_from, created_at)
    VALUES ($1, $2, $3::vector, NOW(), NOW()) RETURNING id),
    similar AS (SELECT sm.id, sm.content FROM semantic_memory sm
    WHERE sm.superseded_by IS NULL AND sm.embedding IS NOT NULL
    AND 1.0 - (sm.embedding <=> $3::vector) > 0.89
    ORDER BY sm.embedding <=> $3::vector ASC LIMIT 1)
    SELECT i.id AS inserted_id, s.id AS similar_id, s.content AS similar_content
    FROM inserted i LEFT JOIN similar s ON true.
    Format embedding param: const embeddingLiteral = '[' + embedding.join(',') + ']'.
    Pass embeddingLiteral as $3. Map result row to { id: rows[0].inserted_id,
    suggestedMerge: rows[0].similar_id ? { id: rows[0].similar_id, content: rows[0].similar_content } : null }.

    supersede: UPDATE semantic_memory SET superseded_by = $2 WHERE id = $1
    ($1 = oldId, $2 = newId). Migration 008 trigger fires valid_until automatically.

    insertProceduralTemplate: add is_anti_pattern as $7 in the INSERT column list.
    SQL: INSERT INTO procedural_memory (scope_id, content, intent_description, template_graph,
    topology_embedding, intent_embedding, success_count, reinforcement_count, last_used_at,
    created_at, is_anti_pattern) VALUES ($1, $2, $3, $4, $5, $6, 0, 0, NOW(), NOW(), $7)
    Add params.isAntiPattern ?? false as the seventh query parameter.

    CHANGE 5 — StubMemoryRepository mirror:
    Add calls.appendEpisodicSummary: Array<{ scopeId: string; entityId: string; intentSummary: string; outcomeSummary: string; embeddingLiteral: string }> = []
    Add calls.supersede: Array<{ oldId: string; newId: string }> = []
    Update calls.insertSemanticFact to Array<{ scopeId: string; content: string; embedding: number[] }> = []
    Add private _suggestedMergeResult: { id: string; content: string } | null = null
    Add setSuggestedMergeResult(result: { id: string; content: string } | null): void setter
    appendEpisodicSummary stub: pushes to calls.appendEpisodicSummary
    insertSemanticFact stub: pushes to calls.insertSemanticFact, returns
    { id: 'stub-id', suggestedMerge: this._suggestedMergeResult }
    supersede stub: pushes { oldId, newId } to calls.supersede
    ProceduralTemplateParams type in StubMemoryRepository.calls.insertProceduralTemplate
    already accepts the extended interface since it references the same ProceduralTemplateParams type.
  </action>

  <acceptance_criteria>
    - memory-repository.ts contains: appendEpisodicSummary(scopeId: string, entityId: string, intentSummary: string, outcomeSummary: string, embeddingLiteral: string): Promise<void>
    - memory-repository.ts contains: insertSemanticFact(scopeId: string, content: string, embedding: number[]): Promise<{ id: string; suggestedMerge:
    - memory-repository.ts contains: supersede(oldId: string, newId: string): Promise<void>
    - memory-repository.ts contains: isAntiPattern?: boolean
    - memory-repository.ts contains: is_anti_pattern) VALUES ($1, $2, $3, $4, $5, $6, 0, 0, NOW(), NOW(), $7)
    - memory-repository.ts contains: calls.supersede
    - memory-repository.ts contains: calls.appendEpisodicSummary
    - memory-repository.ts contains: setSuggestedMergeResult
    - memory-repository.ts does NOT contain the old: insertSemanticFact(scopeId: string, content: string): Promise<void> (signature with only two params before the colon)
  </acceptance_criteria>

  <verify>
    <automated>npm run typecheck 2>&1 | head -20</automated>
  </verify>
  <done>
    MemoryRepository interface, PoolMemoryRepository SQL, and StubMemoryRepository all updated;
    TypeScript compiles without errors related to these files.
  </done>
</task>

<task type="auto">
  <name>Task 3: TrailReader.getScopeEvents() + Worker.shouldReflect()</name>
  <files>packages/workers/src/base/trail-reader.ts, packages/workers/src/base/worker.abstract.ts</files>

  <read_first>
    packages/workers/src/base/trail-reader.ts — full file; note existing query patterns,
    ZERO_HASH import, EventLogNode type import, PoolTrailReader and StubTrailReader structure
    packages/workers/src/base/worker.abstract.ts — full file; note the Phase 08 pipeline
    observability hooks section, protected vs public visibility patterns, JSDoc conventions
  </read_first>

  <action>
    FILE 1 — packages/workers/src/base/trail-reader.ts:

    Add to TrailReader interface after getTailVersionHash:
    getScopeEvents(scopeId: string): Promise<EventLogNode[]>
    JSDoc: "Return all EventLogNode rows for a scope in insertion order (id ASC). Used by
    TemplateProposalWorker to read the full Scope DAG for LLM distillation (D-02)."

    Add to PoolTrailReader after getTailVersionHash implementation:
    async getScopeEvents(scopeId: string): Promise<EventLogNode[]> {
      using query: SELECT * FROM execution_event_log WHERE scope_id = $1 ORDER BY id ASC
      with parameter [scopeId], return rows (typed as EventLogNode[]).
    }

    Add to StubTrailReader after getTailVersionHash implementation:
    async getScopeEvents(_scopeId: string): Promise<EventLogNode[]> { return []; }

    FILE 2 — packages/workers/src/base/worker.abstract.ts:

    Add after the onResultWritten protected hook (line ~125), in the Phase 08 section:
    /**
     * Returns true if this Worker participates in the Reflection Track (D-11, Phase 09).
     * Override to return false to opt out of mem::reflect cold_start injection.
     * Default: true — memory augmentation is on by default for all Workers.
     */
    shouldReflect(): boolean { return true; }

    The method is PUBLIC (not protected) so runContextAssemblyPipeline can call it without
    type-casting through HookCaller. Do not add to the HookCaller local type alias in assemble.ts
    since it is already public on the Worker type.
  </action>

  <acceptance_criteria>
    - trail-reader.ts contains: getScopeEvents(scopeId: string): Promise<EventLogNode[]>
    - trail-reader.ts PoolTrailReader contains: ORDER BY id ASC
    - trail-reader.ts StubTrailReader contains: getScopeEvents(_scopeId: string): Promise<EventLogNode[]> { return []; }
    - worker.abstract.ts contains: shouldReflect(): boolean { return true; }
    - worker.abstract.ts shouldReflect is NOT prefixed with protected or private keyword
    - worker.abstract.ts contains D-11 in the JSDoc comment for shouldReflect
  </acceptance_criteria>

  <verify>
    <automated>npm run typecheck 2>&1 | head -20</automated>
  </verify>
  <done>
    TrailReader.getScopeEvents and Worker.shouldReflect added; TypeScript compiles. All three
    TrailReader implementations (interface, Pool, Stub) have the method.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application→DB | SQL parameters must never contain unescaped user content; all SQL uses parameterized queries |
| embedding API→workers | Embedding vectors from external provider; no content validation needed but failure must not block DB writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Injection | PoolMemoryRepository.appendEpisodicSummary | mitigate | All string params are writeGuard-sanitised at caller (TPW); SQL uses $N parameterised bindings |
| T-09-01-02 | Injection | PoolMemoryRepository.insertSemanticFact Writable CTE | mitigate | Embedding vector formatted as '[v1,v2,...]' string by caller; $3::vector cast in SQL prevents injection |
| T-09-01-03 | Tampering | Migration 012 DDL | accept | DDL runs in controlled migration script; IF NOT EXISTS prevents double-apply; no data deletion |
| T-09-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan — existing pg, pgvector managed by existing lockfile |
</threat_model>

<verification>
Run from repo root:
1. npm run typecheck — zero errors
2. Confirm migrations/012-memory-embedding.sql contains both index names
3. grep -n "appendEpisodicSummary\|supersede\|isAntiPattern" packages/workers/src/base/memory-repository.ts — shows all four additions
4. grep -n "getScopeEvents" packages/workers/src/base/trail-reader.ts — shows interface + PoolTrailReader + StubTrailReader
5. grep -n "shouldReflect" packages/workers/src/base/worker.abstract.ts — shows public method
</verification>

<success_criteria>
- migrations/012-memory-embedding.sql created with idx_episodic_memory_embedding_hnsw and idx_procedural_memory_topology_hnsw_negative
- MemoryRepository interface and both implementations (Pool + Stub) compile with updated signatures
- TrailReader interface and both implementations (Pool + Stub) compile with getScopeEvents
- Worker.shouldReflect() is a public method defaulting to true
- npm run typecheck returns exit code 0
</success_criteria>

<output>
Create .planning/phases/09-memory-layers/09-01-SUMMARY.md when done.
</output>
