# Resume: Phase 09 — Memory Layers (4/4 plans complete, code-review fix DONE)

**Paused:** 2026-06-11 (after CR-01/WR-01/02/03 fix, before execute-phase resume)
**Status:** All 4 plans (09-01..09-04) complete and merged to master. Code review
(070c7dfd) fixes for CR-01 (Critical) + WR-01/WR-02/WR-03 (Warnings) are committed.
Pause occurred before `/gsd:execute-phase 9` was resumed to run the remaining gates.

## Where things stand

- Repo committed through `4122a4db` ("fix(09): correct cold_start detection signal
  per CR-01"). `npm run typecheck` clean, 283/283 tests passing.
- All 4 plans complete and merged: 092be4f4, 51d08868, 3fca5981, 2787c48e.
- Code review committed: `070c7dfd` -> `.planning/phases/09-memory-layers/09-REVIEW.md`.
- **CR-01/WR-01/02/03 fix committed: `4122a4db`**
  - `packages/shared/src/cold-start.ts` (new): `isScopeColdStart(pool, scopeId)` —
    true when `execution_event_log` has exactly 1 row for the scope (first turn).
    This is now the canonical cold_start signal (D-10).
  - `packages/gateway/src/process-agent-turn.ts`: uses `isScopeColdStart` instead
    of the broken `episodic_memory` count check (CR-01); `extractQueryText()` pulls
    a short descriptive field from the payload instead of full JSON (WR-03).
  - `packages/workers/src/context/assemble.ts`: `opts.memReflect.hasEpisodic` ->
    `isColdStart` (WR-01) — still has zero production callers (IN-02, deferred).
  - `packages/workers/src/memory/reflect.function.ts`: `formatProcedural` no longer
    overruns its budget on the last entry (WR-02).

## Next action

Resume `/gsd:execute-phase 9` (or `/gsd:resume-work`). `09-04-SUMMARY.md` exists and
the CR-01 blocker is now resolved, so the orchestrator should skip straight to:

1. `regression_gate` (re-run typecheck+test if not trusting this session's run —
   already verified clean as of `4122a4db`)
2. `schema_drift_gate`
3. `codebase_drift_gate`
4. `verify_phase_goal` (spawn `gsd-verifier`)
5. `update_roadmap` (mark Phase 9 complete 4/4)
6. `close_phase_todos`
7. `update_project_md`
8. `offer_next`

## Decisions carried forward

- D-08: No auto-supersede — caller explicitly calls `supersede()` only when
  `insertSemanticFact` returns non-null `suggestedMerge`.
- `occWriteIdempotent` must stay exported from `packages/shared/src/occ-write.ts`.
- IN-02 (assemble.ts `opts.memReflect` has zero callers) remains intentionally
  deferred — documented via the `isColdStart` rename + doc comment pointing to
  `isScopeColdStart` as the canonical implementation when wired up later.
- Untracked dirs (`06-gateway-seam-extraction/`, `08-REVIEW-FIX.md`,
  `11-DESIGN-NOTES.md`, `.planning/tmp/`, `data/`) are pre-existing/unrelated —
  do not commit as part of Phase 9 closeout unless instructed.

## Resume command

`/gsd:resume-work` or `/gsd:execute-phase 9`.
