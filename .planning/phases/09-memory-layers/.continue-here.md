# Resume: Phase 09 — Memory Layers (4/4 plans complete, code-review fix pending)

**Paused:** 2026-06-11T04:09:25.151Z
**Status:** All 4 plans (09-01..09-04) complete and merged to master. Code review found a Critical
correctness bug (CR-01) in the production cold_start wiring; user chose "Fix now" before phase
verification. Pause occurred before the code-fixer was spawned.

## Where things stand

- Repo committed through `070c7dfd` (09-REVIEW.md). `npm run typecheck` clean, 283/283 tests
  passing as of the 09-04 merge (`2787c48e`).
- All 4 plans complete and merged:
  - 09-01 (Wave 1): Migration 012 + MemoryRepository seam + `TrailReader.getScopeEvents()` +
    `Worker.shouldReflect()`. Commit `092be4f4`.
  - 09-02 (Wave 2): TemplateProposalWorker. Commit `51d08868`.
  - 09-03 (Wave 2): SemanticMemoryWorker embeddingProvider + supersede. Commit `3fca5981`.
  - 09-04 (Wave 3): `reflect.function.ts` (memReflect, RRF hybrid search), `assemble.ts`
    cold_start opts, `index.ts` rewiring (TPW registration, EpisodicMemoryWorker removal,
    SemanticMemoryWorker 5th arg, `mem::reflect` iii Function), `process-agent-turn.ts`
    production cold_start wiring, `pulse-fetch.ts` dead-trigger cleanup. Commit `2787c48e`.
- Tracking updated: `ef24e3eb` (ROADMAP/STATE — Phase 9 plan progress 4/4).
- Code review committed: `070c7dfd` → `.planning/phases/09-memory-layers/09-REVIEW.md`
  (standard depth, 9 files): **1 Critical (CR-01), 3 Warnings (WR-01/02/03), 3 Info**.

## Next action

1. Spawn `gsd-code-fixer` scoped to `09-REVIEW.md`, default scope (Critical + Warning:
   CR-01, WR-01, WR-02, WR-03).
   - **For CR-01, read first:** `.planning/phases/09-memory-layers/09-CONTEXT.md` and
     `docs/adr/0022-adr21-reflection-track-trigger-spec.md` to confirm the intended cold_start
     signal — this may be a small design clarification (D-10), not a purely mechanical fix.
2. Re-run `npm run typecheck && npm test` — confirm clean (baseline 283/283, 0 typecheck errors).
3. Commit the fix(es), e.g. `fix(09): correct cold_start detection signal per CR-01`.
4. Resume `/gsd:execute-phase 9` — `09-04-SUMMARY.md` exists so the orchestrator should skip
   straight to: `regression_gate` → `schema_drift_gate` → `codebase_drift_gate` →
   `verify_phase_goal` (spawn `gsd-verifier`) → `update_roadmap` (mark Phase 9 complete 4/4) →
   `close_phase_todos` → `update_project_md` → `offer_next`.

## Critical Anti-Patterns

| # | Pattern | Severity | Prevention |
|---|---------|----------|------------|
| 1 | **CR-01 — cold_start gate never fires correctly.** `process-agent-turn.ts:78-93`'s `SELECT COUNT(*) FROM episodic_memory WHERE scope_id=$1; if cnt==='0'` is always true mid-scope (episodic_memory is only populated by `TemplateProposalWorker.onScopeClosed` on `graph::scope::closed`; `appendEpisodicTrace` has zero production callers). Result: `memReflect` (1 embedding call + 3 hybrid SQL searches) fires on **every turn**, not cold_start-only — violates D-10. | blocking | Do not "fix" by just changing the table queried without first confirming the intended cold_start signal in `09-CONTEXT.md` / ADR-0022. The fix must define what "cold_start" means operationally (e.g. trail event count == 0 for the scope via `TrailReader.getScopeEvents()`, or an explicit first-turn flag) and apply that signal consistently in BOTH `process-agent-turn.ts` (production path) AND `assemble.ts`'s `opts.memReflect.hasEpisodic` callback (WR-01) so the two checks don't drift. |
| 2 | Worktree merge from a stale base can silently revert unrelated `.harness/*` and `.planning/STATE.md`/`ROADMAP.md` files (happened during 09-03 merge; fixed). | advisory | After every `git merge <worktree-branch> --no-ff`, run `git diff <pre-merge-master-sha>..HEAD --stat` and check for ANY file outside the plan's `files_modified`. Confirmed clean for the 09-04 merge. |
| 3 | Test fixtures using literal event-type strings like `'graph::scope::opened'` will fail TS2322 — `CanonicalEventType` only allows `plan_created \| task_spawned \| memory_updated \| conflict_detected \| scope_closed`. | advisory | If the CR-01 fix touches test fixtures, use the 5 canonical values only. |

## Decisions carried forward (full list also in HANDOFF.json)

- D-08: No auto-supersede — caller explicitly calls `supersede()` only when
  `insertSemanticFact` returns non-null `suggestedMerge`.
- `occWriteIdempotent` must stay exported from `packages/shared/src/occ-write.ts`.
- WR-02 (formatProcedural budget overrun) and WR-03 (full-payload query_text) are independent
  of CR-01 but in the same fix batch (default code-fixer scope = Critical + Warning).
- IN-02 noted `opts.memReflect` injection point in `assemble.ts` appears to have no caller in
  the 09-04 diff — confirm whether this is intentional (deferred wiring) or a missed connection
  while addressing WR-01.

## Resume command

`/gsd:resume-work` or `/gsd-execute-phase 9` (will detect 4/4 plans done with `09-04-SUMMARY.md`
present, but should NOT mark phase complete until the CR-01 fix lands and verification passes —
read this file's blocking constraint first).
