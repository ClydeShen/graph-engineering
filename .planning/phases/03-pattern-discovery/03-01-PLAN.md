---
phase: 03-pattern-discovery
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - migrations/007-pattern-discovery-mcp.sql
  - packages/workers/src/memory/procedural.worker.ts
  - packages/workers/src/index.ts
  - packages/shared/src/constants.ts
  - packages/workers/src/memory/wl-embedding.test.ts
  - packages/workers/src/patterns/cross-scope.test.ts
  - packages/gateway/src/routes/mcp.test.ts
autonomous: true
requirements: [GATE4-1, GATE4-2]
must_haves:
  truths:
    - "agent_registry table exists with GIN index on skills[] and status/heartbeat partial index"
    - "procedural_memory has intent_embedding vector(1536) and cross_domain_cluster_id UUID columns"
    - "ProceduralMemoryWorker writes a non-NULL intent_embedding on every template INSERT"
    - "Migration 007 is idempotent — re-running raises no error"
    - "Wave-0 test scaffolds exist and fail (RED) until their implementation plans land"
  artifacts:
    - path: "migrations/007-pattern-discovery-mcp.sql"
      provides: "agent_registry table + intent_embedding + cross_domain_cluster_id columns"
      contains: "CREATE TABLE IF NOT EXISTS agent_registry"
    - path: "packages/shared/src/constants.ts"
      provides: "AGENT_HEARTBEAT_TTL_S constant"
      contains: "AGENT_HEARTBEAT_TTL_S"
    - path: "packages/workers/src/patterns/cross-scope.test.ts"
      provides: "GATE4-2 union-find cluster assignment test scaffold"
      contains: "cross_domain_cluster_id"
  key_links:
    - from: "migrations/007-pattern-discovery-mcp.sql"
      to: "agent_registry"
      via: "CREATE INDEX ... USING GIN (skills)"
      pattern: "USING GIN \\(skills\\)"
    - from: "packages/workers/src/memory/procedural.worker.ts"
      to: "procedural_memory.intent_embedding"
      via: "INSERT includes intent_embedding column with non-NULL vector literal"
      pattern: "intent_embedding"
---

<objective>
Establish the shared schema foundation for both Phase 3 streams: migration 007 creates `agent_registry` (Stream B skill-matching) and adds `intent_embedding` + `cross_domain_cluster_id` to `procedural_memory` (Stream A cross-domain clustering). Also close the RESEARCH-confirmed gap where `intent_embedding` does not exist in any migration and `ProceduralMemoryWorker` never writes it — without populated `intent_embedding`, the cross-domain guard query (`intent_embedding <=> b.intent_embedding > 0.50`) has no data to filter on and GATE4-2 cannot pass.

Purpose: Both streams depend on this schema. FrontierScheduler skill-matching (Plan 03) reads `agent_registry`. CrossScopePatternDiscoveryWorker (Plan 02) reads `intent_embedding` and writes `cross_domain_cluster_id`. This is Wave 0 — it blocks everything.

Output: `migrations/007-pattern-discovery-mcp.sql`, an extended `ProceduralMemoryWorker.onSynthesizerOutput` that computes and writes `intent_embedding`, the `AGENT_HEARTBEAT_TTL_S` constant, and the four Wave-0 test scaffolds (RED) that downstream implementation plans turn GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-pattern-discovery/03-RESEARCH.md
@.planning/phases/03-pattern-discovery/03-CONTEXT.md
@.harness/phases/side-branch/DESIGN.md
@docs/adr/0027-adr25-cross-domain-topology-algorithm.md
@docs/adr/0039-adr37-pattern-discovery-schedule.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 007-pattern-discovery-mcp.sql</name>
  <files>migrations/007-pattern-discovery-mcp.sql</files>

  <read_first>
    migrations/006-memory-extensions.sql — idempotent ALTER style and comment header convention to match exactly (ADD COLUMN IF NOT EXISTS, CREATE INDEX IF NOT EXISTS, `── table ──` dividers)
    migrations/003-memory-tables.sql — confirms procedural_memory already has topology_embedding vector(128), is_anti_pattern, quality_score; confirms NO intent_embedding and NO cross_domain_cluster_id exist (do not duplicate)
    .harness/phases/side-branch/DESIGN.md §3.2 — canonical agent_registry DDL (column names, CHECK constraints, GIN index)
    .planning/phases/03-pattern-discovery/03-RESEARCH.md §"Migration 007" code example — full reference DDL including the status/heartbeat partial index
    docs/adr/0027-adr25-cross-domain-topology-algorithm.md — confirms topology_embedding is vector(128); intent_embedding must match the semantic embedding dimension used elsewhere
    migrations/001-extensions.sql — confirms pgvector + pgcrypto extensions are enabled (vector type + gen_random_uuid available)
  </read_first>

  <action>
  Create `migrations/007-pattern-discovery-mcp.sql` following the comment header + section divider style of `migrations/006-memory-extensions.sql` exactly.

  Section 1 — agent_registry table (per DESIGN.md §3.2). Use `CREATE TABLE IF NOT EXISTS agent_registry` with columns: `agent_id UUID PRIMARY KEY DEFAULT gen_random_uuid()`, `name TEXT NOT NULL`, `description TEXT`, `skills TEXT[] NOT NULL DEFAULT '{}'`, `protocol TEXT NOT NULL CHECK (protocol IN ('mcp','a2a','iii'))`, `endpoint TEXT`, `agent_card_json JSONB NOT NULL`, `registered_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`, `last_heartbeat TIMESTAMPTZ`, `status TEXT NOT NULL DEFAULT 'active' CHECK (status IN ('active','inactive'))`.

  Then `CREATE INDEX IF NOT EXISTS idx_agent_registry_skills ON agent_registry USING GIN (skills)` and `CREATE INDEX IF NOT EXISTS idx_agent_registry_status_heartbeat ON agent_registry (status, last_heartbeat) WHERE status = 'active'`.

  Section 2 — procedural_memory extensions. `ALTER TABLE procedural_memory ADD COLUMN IF NOT EXISTS intent_embedding vector(1536)` (dimension 1536 matches the OpenAI-compatible semantic embedding used by SemanticMemoryWorker; the cross-domain guard compares intent_embedding via the `<=>` cosine operator). `ALTER TABLE procedural_memory ADD COLUMN IF NOT EXISTS cross_domain_cluster_id UUID`. Then `CREATE INDEX IF NOT EXISTS idx_procedural_cross_domain_cluster ON procedural_memory (cross_domain_cluster_id) WHERE cross_domain_cluster_id IS NOT NULL`.

  Do NOT add an HNSW index on intent_embedding — the cross-domain query loads candidate rows by topology_embedding HNSW first, then filters intent in-process or via the `<=>` operator on the small candidate set (RESEARCH Pitfall 1). A separate HNSW on intent_embedding is unnecessary and would slow INSERTs.

  Add a closing comment documenting that intent_embedding is nullable for existing Phase 2 rows (those rows predate this column; CrossScopePatternDiscovery skips rows where intent_embedding IS NULL per its WHERE clause).
  </action>

  <verify>
    <automated>psql $DATABASE_URL -f migrations/007-pattern-discovery-mcp.sql && echo "migration OK"</automated>
    <automated>psql $DATABASE_URL -f migrations/007-pattern-discovery-mcp.sql && echo "idempotent OK"</automated>
    <automated>psql $DATABASE_URL -c "\d agent_registry" | grep -E "skills|protocol|agent_card_json"</automated>
    <automated>psql $DATABASE_URL -c "SELECT indexname FROM pg_indexes WHERE tablename='agent_registry' AND indexname='idx_agent_registry_skills'"</automated>
    <automated>psql $DATABASE_URL -c "\d procedural_memory" | grep -E "intent_embedding|cross_domain_cluster_id"</automated>
  </verify>

  <acceptance_criteria>
  - `\d agent_registry` lists columns: agent_id (uuid), name (text), skills (ARRAY/text[]), protocol (text), agent_card_json (jsonb), last_heartbeat (timestamptz), status (text)
  - `SELECT indexname FROM pg_indexes WHERE indexname='idx_agent_registry_skills'` returns exactly one row, and its indexdef contains `gin` and `skills`
  - `SELECT indexname FROM pg_indexes WHERE indexname='idx_agent_registry_status_heartbeat'` returns one row
  - `\d procedural_memory` contains `intent_embedding` (type vector / vector(1536)) and `cross_domain_cluster_id` (type uuid)
  - `SELECT indexname FROM pg_indexes WHERE indexname='idx_procedural_cross_domain_cluster'` returns one row
  - Running migration 007 twice produces no error (all statements use IF NOT EXISTS)
  - The string `topology_embedding` does NOT appear in migration 007 (it already exists in migration 003 — must not be re-declared)
  </acceptance_criteria>

  <done>Migration 007 applies cleanly to a database that has run migrations 001-006, is idempotent, creates agent_registry with the GIN skills index, and adds intent_embedding + cross_domain_cluster_id to procedural_memory.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Populate intent_embedding in ProceduralMemoryWorker + add AGENT_HEARTBEAT_TTL_S</name>
  <files>packages/workers/src/memory/procedural.worker.ts, packages/workers/src/index.ts, packages/shared/src/constants.ts</files>

  <read_first>
    packages/workers/src/memory/procedural.worker.ts — current onSynthesizerOutput signature and INSERT statement (positional params $1..$5; topology_embedding is $5). The INSERT must be extended to add intent_embedding.
    packages/workers/src/memory/procedural.worker.test.ts — existing unit tests asserting topology_embedding is $5 and computeWLEmbedding is called; the new test must follow the same mock/param-inspection style and existing assertions must remain valid
    packages/workers/src/index.ts §graph::memory::procedural — how onSynthesizerOutput is invoked and which payload fields are available (intent_description is present; nodes/edges are present)
    packages/shared/src/constants.ts — exact export style (export const NAME = value with ADR-cited JSDoc); add AGENT_HEARTBEAT_TTL_S here
    packages/workers/src/memory/semantic.worker.ts — how the OpenAI-compatible provider produces a 1536-dim embedding from text (reuse the same embedding call so intent_embedding dimension matches semantic_memory)
    docs/adr/0027-adr25-cross-domain-topology-algorithm.md — confirms intent_embedding is the semantic embedding of the intent_description (NOT the WL topology embedding)
  </read_first>

  <behavior>
    - onSynthesizerOutput computes intent_embedding from intent_description via the injected LLM provider's embedding method (1536-dim), formats it as a bracketed pgvector literal `[v0,v1,...]`
    - The INSERT into procedural_memory now includes the intent_embedding column with the bracketed literal as a positional param
    - topology_embedding remains a separate, distinct value (WL kernel output) — the two embeddings are NOT the same vector
    - Existing test "inserts ... with topology_embedding as $5" still passes (topology_embedding position unchanged or test updated to new position with explicit assertion)
    - AGENT_HEARTBEAT_TTL_S is exported as a number constant (value: Claude's discretion per CONTEXT.md — recommended 60)
  </behavior>

  <action>
  Add `intent_embedding` population to `ProceduralMemoryWorker`. The worker constructor currently takes only `pool`; inject the LLM provider so the worker can compute the semantic embedding of `intentDescription`. Update the constructor to `constructor(pool: Pool, llm: LLMProvider)` and update the registration in `packages/workers/src/index.ts` (`new ProceduralMemoryWorker(pool)` → `new ProceduralMemoryWorker(pool, llmProvider)` — `llmProvider` already exists in that file).

  In `onSynthesizerOutput`, after computing `embeddingLiteral` (topology), call the provider's embedding method on `intentDescription` to get a 1536-dim Float array, format it as `[${arr.join(',')}]` into `intentEmbeddingLiteral`. Extend the INSERT column list to include `intent_embedding` and add the literal as a new positional param. Keep the HNSW guard comment style.

  If the LLM provider embedding call fails or returns empty, write `NULL` for intent_embedding (the cross-domain query skips NULL rows) — do not throw; topology_embedding must still be written (it must never be NULL per Pitfall 5).

  Add `AGENT_HEARTBEAT_TTL_S` to `packages/shared/src/constants.ts` with an ADR-37/DESIGN-cited JSDoc comment, value `60` (seconds) — agent_registry staleness threshold used by FrontierScheduler skill-matching (Plan 03) and the heartbeat eviction query.

  Add a unit test case to `procedural.worker.test.ts` asserting the INSERT params include a bracketed intent_embedding literal distinct from the topology literal, and that the provider embedding method was called with `intentDescription`.
  </action>

  <verify>
    <automated>npx vitest run packages/workers/src/memory/procedural.worker.test.ts</automated>
    <automated>npm run typecheck</automated>
    <automated>node -e "const c=require('./packages/shared/src/constants.ts'); " 2>nul || grep -c "AGENT_HEARTBEAT_TTL_S" packages/shared/src/constants.ts</automated>
  </verify>

  <acceptance_criteria>
  - `procedural.worker.test.ts` passes including a new case asserting intent_embedding is present in INSERT params as a bracketed literal AND differs from the topology_embedding literal
  - `grep -v '^#' packages/shared/src/constants.ts | grep -c "AGENT_HEARTBEAT_TTL_S"` returns 1 or more
  - `npm run typecheck` exits 0 (ProceduralMemoryWorker constructor change propagated to index.ts and all callers)
  - The INSERT statement in procedural.worker.ts column list contains `intent_embedding`
  - topology_embedding is still always written (never NULL); intent_embedding may be NULL only on provider failure
  </acceptance_criteria>

  <done>ProceduralMemoryWorker writes both topology_embedding and intent_embedding on each template INSERT, the constructor accepts an LLM provider wired in index.ts, AGENT_HEARTBEAT_TTL_S is exported, and all worker unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Create Wave-0 RED test scaffolds for GATE4-1, GATE4-2, GATE4-4</name>
  <files>packages/workers/src/memory/wl-embedding.test.ts, packages/workers/src/patterns/cross-scope.test.ts, packages/gateway/src/routes/mcp.test.ts</files>

  <read_first>
    packages/workers/src/memory/wl-embedding.test.ts — existing tests for computeWLEmbedding (Float32Array(128), L2-normalized, determinism). Add a GATE4-1 case; do not remove existing cases.
    packages/workers/src/memory/gate3.integration.test.ts — the `skipIf(skip)` DB-gating pattern (skip when DATABASE_URL is absent) to replicate in cross-scope.test.ts and mcp.test.ts
    packages/gateway/src/routes/health.test.ts — Hono route test bootstrapping pattern (how buildApp / a route is mounted and exercised in-process)
    packages/workers/src/memory/wl-embedding.ts — computeWLEmbedding signature (nodes, edges) so the GATE4-1 test can construct two isomorphic-but-relabeled graphs
    .planning/phases/03-pattern-discovery/03-RESEARCH.md §"Phase Requirements → Test Map" — exact command paths and gating expectations for GATE4-1, GATE4-2, GATE4-4
  </read_first>

  <action>
  Create three test files that establish the RED baseline for Gate 4 criteria. They are scaffolds: downstream plans turn them GREEN.

  1. Extend `packages/workers/src/memory/wl-embedding.test.ts` (GATE4-1): add a test that builds two topologically equivalent graphs from different "domains" (same node-degree structure and same edge topology, but different node id labels), computes both embeddings via `computeWLEmbedding`, and asserts cosine similarity > 0.90. Compute cosine inline (dot product over L2-normalized vectors). This test should PASS already if the WL kernel is label-invariant on structure — if it FAILS, it reveals that the kernel keys on raw labels and Plan 02 must address it. Either outcome is a valid Wave-0 signal; mark the expectation clearly.

  2. Create `packages/workers/src/patterns/cross-scope.test.ts` (GATE4-2): unit test for the union-find cluster assignment logic. Import the (not-yet-existing) `discoverClusters` / cluster-assignment export from `./cross-scope.js`. Use `it.skipIf(!process.env.DATABASE_URL)` for the DB-backed assertion that `cross_domain_cluster_id` is written, AND a pure unit test (no DB) for the union-find function that groups pair lists into cluster IDs. The DB test asserts: after running discovery on two topologically-similar/intent-distant rows, both rows share a non-NULL cross_domain_cluster_id. This file will fail to import until Plan 02 creates cross-scope.ts — that is the intended RED state.

  3. Create `packages/gateway/src/routes/mcp.test.ts` (GATE4-4): integration scaffold. Use `it.skipIf(!process.env.DATABASE_URL)`. Assert that the MCP route (not yet existing) responds to a `tools/list` JSON-RPC POST with the 7 tool names, and that a `spawn_subtask` → `claim_next_task` → `complete_task` sequence writes the expected events. Reference the route via the gateway buildApp pattern. This fails until Plan 05 creates the MCP route — intended RED.

  Do NOT implement cross-scope.ts or mcp.ts in this plan. These are RED scaffolds only. Each file must contain a top comment naming the GATE4 criterion and the plan that turns it GREEN.
  </action>

  <verify>
    <automated>npx vitest run packages/workers/src/memory/wl-embedding.test.ts</automated>
    <automated>npx vitest run packages/workers/src/patterns/cross-scope.test.ts 2>&1 | grep -E "fail|cannot find|RED|skipped" || echo "scaffold present"</automated>
    <automated>test -f packages/gateway/src/routes/mcp.test.ts && echo "mcp scaffold present"</automated>
  </verify>

  <acceptance_criteria>
  - `packages/workers/src/memory/wl-embedding.test.ts` contains a GATE4-1 cosine-similarity > 0.90 test over two relabeled-isomorphic graphs; all pre-existing tests in the file still pass
  - `packages/workers/src/patterns/cross-scope.test.ts` exists, contains a pure union-find unit test and a `skipIf(!DATABASE_URL)` cross_domain_cluster_id assertion, and references `./cross-scope.js`
  - `packages/gateway/src/routes/mcp.test.ts` exists, is DB-gated with skipIf, and asserts the 7 MCP tool names and a spawn→claim→complete sequence
  - Each scaffold file's top comment names its GATE4 criterion and the plan number that turns it GREEN
  - cross-scope.ts and mcp.ts source files are NOT created in this plan
  </acceptance_criteria>

  <done>Three Wave-0 test scaffolds exist establishing the RED baseline for GATE4-1, GATE4-2, and GATE4-4; the GATE4-1 test runs against the existing WL kernel; the GATE4-2 and GATE4-4 tests reference not-yet-built modules and are the explicit RED targets for Plans 02 and 05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DBA → PostgreSQL | Migration 007 runs with DDL rights; creates a new table and alters procedural_memory |
| LLM provider → Worker | ProceduralMemoryWorker sends intent_description text to the embedding endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | migrations/007-pattern-discovery-mcp.sql | mitigate | IF NOT EXISTS guards on every statement prevent destructive re-runs; file committed and reviewed before apply |
| T-03-01-02 | Information Disclosure | intent_description → LLM embedding endpoint | accept | intent_description is already sent to the same provider by SemanticMemoryWorker; no new data crosses the boundary |
| T-03-01-03 | Denial of Service | LLM embedding call inside synchronous INSERT path | mitigate | Embedding failure falls back to NULL intent_embedding; topology write still succeeds; no throw blocks the template write |
| T-03-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan — pure SQL + TypeScript on existing deps |
</threat_model>

<verification>
- `npm run typecheck` exits 0
- `npx vitest run packages/workers/src/memory` is green except the intentionally-RED cross-scope DB test (which is skipped without DATABASE_URL)
- Migration 007 applied to a DB at migration 006 state leaves all Phase 2 data intact (`SELECT count(*) FROM procedural_memory` unchanged)
</verification>

<success_criteria>
1. Migration 007 creates agent_registry (GIN skills index + status/heartbeat index) and adds intent_embedding vector(1536) + cross_domain_cluster_id UUID to procedural_memory, idempotently.
2. ProceduralMemoryWorker writes a non-NULL intent_embedding on every successful template INSERT (NULL only on provider failure).
3. AGENT_HEARTBEAT_TTL_S is exported from shared constants.
4. Wave-0 RED scaffolds exist for GATE4-1, GATE4-2, GATE4-4.
</success_criteria>

<output>
Create `.planning/phases/03-pattern-discovery/03-01-SUMMARY.md` when done.
</output>
