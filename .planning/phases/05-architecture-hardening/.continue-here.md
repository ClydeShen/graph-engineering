---
context: phase
phase: 05-architecture-hardening
task: 0
total_tasks: 6
status: ready_to_execute
last_updated: 2026-06-09T23:55:00.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints from this session — planning completed cleanly._

<current_state>
Phase 5 "Architecture Hardening" planning is COMPLETE. All 6 ARCH plans are written, checker-verified, and committed (ad72735). Ready to execute.

Two-tracking-system reminder: harness (.harness/) uses phases 04-07; GSD (.planning/) uses phases 1-5. They are INDEPENDENT — do not reconcile numbering. GSD Phase 5 = harness Phase 07-architecture remaining work.
</current_state>

<completed_work>

This session (planning only — no code written):
- Research: manual analysis of nanobot + hermes-agent specimen projects for ARCH-01..06 patterns
- RESEARCH.md written to `.planning/phases/05-architecture-hardening/RESEARCH.md`
- PATTERNS.md written by gsd-pattern-mapper agent (9 file analogs, all found)
- 6 PLAN.md files written by gsd-planner agent
- Plan-checker ran: 5 blockers found, all 5 fixed:
  1. 05-02: pg client try/finally for SSE stream close (leak prevention)
  2. 05-03: CanonicalEventType documented as controlled duplicate (not a real constraint violation)
  3. 05-05: wave 2 + depends_on:['02'] (resolved gateway/src/index.ts conflict with 05-02)
  4. 05-05: path traversal validation added to GET /v1/skills/:id action
  5. 05-06: node:crypto scope clarified (applies only to version_hash chain, not fingerprints)
- All committed: ad72735
</completed_work>

<remaining_work>

- Execute all 6 ARCH plans: `/gsd-execute-phase 5`
- Wave 1 (parallel, disjoint file sets): 05-01, 05-02, 05-03, 05-04
- Wave 2 (after Wave 1): 05-05 (depends on 05-02 for gateway/src/index.ts), 05-06

After execution:
- Update .planning/ROADMAP.md Phase 5 plan count (0 → 6)
- Update .harness/phases/07-architecture/.continue-here.json
- Update .harness/state.json
</remaining_work>

<decisions_made>

- DRY from hermes-agent: `FailoverReason` enum + `classifyProviderError()` ported to TypeScript for ARCH-01
- DRY from hermes-agent: `try/finally { client.release() }` pattern for SSE pg client lifecycle
- DRY from hermes-agent: two-phase skill bundle loading (frontmatter-first) for ARCH-05
- DRY from hermes-agent: delta injection pattern (existing_lesson → LLM outputs delta only) for ARCH-06
- `@graph/types` is a leaf package — zero @graph/* deps to prevent circular imports
- CanonicalEventType in @graph/types/api is an intentional inline union (cannot import from @graph/shared without creating circular dep); documented as known controlled duplicate
- `node:crypto` for lesson fingerprints is NOT a violation of the pgcrypto constraint — that constraint applies to the OCC version_hash chain (ADR 02) only; lesson-save.worker.ts already uses node:crypto as precedent
- 05-05 is Wave 2 (depends on 05-02) because both modify `packages/gateway/src/index.ts`
- `~/.memex/config.json` returns null when absent — system boots from env vars
</decisions_made>

<blockers>
None. All plan-checker blockers resolved.
</blockers>

## Required Reading (in order)
1. `.planning/phases/05-architecture-hardening/RESEARCH.md` — specimen analysis; DRY patterns for ARCH-01..06
2. `.planning/phases/05-architecture-hardening/PATTERNS.md` — exact file analogs for each new file
3. `.planning/ROADMAP.md` Phase 5 section — success criteria (ARCH-01..ARCH-06)
4. `.harness/phases/07-architecture/.continue-here.json` — harness-side context

## Infrastructure State
- No running services needed for planning
- For execution: PostgreSQL at `postgres://postgres:password@localhost:5432/graph_test`
- Worker startup: `C:\Temp\start-workers.cmd`
- Ctrl startup: `C:\Temp\start-ctrl.cmd`

<context>
Planning session: researched nanobot (Python prototype) and hermes-agent (second-gen Memex analog) to find reusable patterns before planning ARCH-01..06. Key findings: hermes-agent has a mature error classifier (`FailoverReason` enum) that maps directly to ARCH-01's circuit-breaker spec; its curator.py delta injection pattern maps to ARCH-06; its skill_bundles.py lazy loading maps to ARCH-05. All patterns incorporated into plans. Checker verified, 5 issues found and fixed. Plans committed.
</context>

<next_action>
Run `/gsd-execute-phase 5` to execute all 6 ARCH plans. Start with Wave 1 (01, 02, 03, 04 in parallel). After Wave 1 completes, run Wave 2 (05, 06).
</next_action>
