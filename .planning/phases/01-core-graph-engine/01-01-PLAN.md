---
phase: 01-core-graph-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vitest.config.ts
  - iii-config.yaml
  - packages/shared/package.json
  - packages/shared/src/canonical-json.ts
  - packages/shared/src/constants.ts
  - packages/shared/src/types.ts
  - packages/shared/src/schemas.ts
  - src/__tests__/canonical-json.test.ts
  - tests/helpers/pg-test-pool.ts
autonomous: true
requirements: [REQ-03]
must_haves:
  truths:
    - "npm run test:unit executes and the canonical-json suite passes"
    - "canonicalJson produces identical output for the same payload regardless of key insertion order"
    - "canonicalJson does not double-encode array elements"
    - "npx tsc --noEmit exits 0 across the workspace"
  artifacts:
    - path: "packages/shared/src/canonical-json.ts"
      provides: "canonicalJson + hashablePayload BTreeMap serialization"
      contains: "Object.keys"
    - path: "package.json"
      provides: "workspace root with test:unit / test:integration scripts and deps"
      contains: "test:unit"
    - path: "vitest.config.ts"
      provides: "vitest configuration"
    - path: "iii-config.yaml"
      provides: "iii engine + worker + LLM provider configuration"
  key_links:
    - from: "src/__tests__/canonical-json.test.ts"
      to: "packages/shared/src/canonical-json.ts"
      via: "import { canonicalJson, hashablePayload }"
      pattern: "canonical-json"
---

<objective>
Establish the TypeScript monorepo scaffold, tooling (tsconfig strict, vitest, test scripts), the `iii-config.yaml`, and the shared package containing the load-bearing `canonicalJson` implementation plus shared types/constants/Zod schemas. This is Wave 0 + scaffold: it creates the test harness stubs that every later plan verifies against.

Purpose: Every downstream plan imports from `packages/shared` and runs `npm run test:unit`. The canonical_json correctness (REQ-03) is the integrity foundation of the entire hash chain — a bug here corrupts all OCC semantics permanently.
Output: Buildable workspace, passing canonical-json unit tests, shared types/schemas/constants consumed by all later plans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-graph-engine/01-CONTEXT.md
@.planning/phases/01-core-graph-engine/01-RESEARCH.md
@.planning/phases/01-core-graph-engine/01-VALIDATION.md
@CLAUDE.md
@CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Workspace scaffold + tooling</name>
  <files>package.json, tsconfig.json, vitest.config.ts, packages/shared/package.json, iii-config.yaml</files>
  <read_first>
    - .planning/phases/01-core-graph-engine/01-RESEARCH.md (Standard Stack table, Recommended Project Structure, Pattern 8 iii-config.yaml, installation command)
    - .planning/phases/01-core-graph-engine/01-VALIDATION.md (Test Infrastructure, package.json test scripts requirement)
    - CLAUDE.md (Simplicity First, conventions)
  </read_first>
  <action>
    Create npm workspace root `package.json` with `"workspaces": ["packages/*"]` and dependencies pinned to RESEARCH.md versions: `iii-sdk@0.17.0`, `pg@8.21.0`, `pg-listen@1.7.0`, `hono@4.12.23`, `zod@4.4.3`, `@dqbd/tiktoken@1.0.22`, `@hono/zod-validator@0.8.0`; devDependencies `tsx@4.22.4`, `typescript@^5`, `vitest`, `@types/pg`, `@types/node`. Add scripts: `"test:unit": "vitest run src/__tests__"`, `"test:integration": "vitest run tests/integration"`, `"test": "vitest run"`, `"typecheck": "tsc --noEmit"`, `"build": "tsc"`. Create `tsconfig.json` with `"strict": true`, `"target": "ES2022"`, `"module": "ESNext"`, `"moduleResolution": "Bundler"`, path alias `"@shared/*": ["packages/shared/src/*"]`. Create `vitest.config.ts` exporting defineConfig with `test.globals: true`, `test.environment: "node"`. Create `packages/shared/package.json` (name `@graph/shared`, type module, main `src/index.ts`). Create `iii-config.yaml` per RESEARCH.md Pattern 8 exactly: workers (iii-worker-manager, iii-http, iii-cron with kv adapter, iii-observability, iii-exec spawning `node dist/control-plane/index.js` and `node dist/workers/index.js`), and `llm` + `embedding` blocks using `openai_compatible` provider with env-var interpolation. Use cross-platform `node` exec commands only (Pitfall 6).
  </action>
  <verify>
    <automated>cd D:/Repo/graph-enginerring && npm install --no-audit --no-fund && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"test:unit"`, `"iii-sdk": "0.17.0"`, `"@hono/zod-validator": "0.8.0"`, and `"workspaces"`
    - `tsconfig.json` contains `"strict": true` and the `@shared/*` path alias
    - `vitest.config.ts` exists and `npx tsc --noEmit` exits 0
    - `iii-config.yaml` contains `provider: openai_compatible`, `iii-cron`, and `node dist/control-plane/index.js`
    - `iii-config.yaml` contains NO shell variable injection of the `VAR=value cmd` form (cross-platform safety)
  </acceptance_criteria>
  <done>Workspace installs, typechecks clean, iii-config.yaml present with all worker blocks and LLM provider config.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: canonicalJson + shared types/constants/schemas (REQ-03)</name>
  <files>packages/shared/src/canonical-json.ts, packages/shared/src/constants.ts, packages/shared/src/types.ts, packages/shared/src/schemas.ts, packages/shared/src/index.ts, src/__tests__/canonical-json.test.ts</files>
  <read_first>
    - .planning/phases/01-core-graph-engine/01-RESEARCH.md (Pattern 9 corrected canonicalJson, Pitfall 1 double-encoding bug, Pattern 3 Zod regexes)
    - CONTEXT.md (Version Hash formula, hashable domain = payload minus _meta minus schema_version)
    - .planning/REQUIREMENTS.md (REQ-03 exact wording)
  </read_first>
  <behavior>
    - canonicalJson({b:1,a:2}) === canonicalJson({a:2,b:1}) (key-order independence)
    - canonicalJson([{b:1,a:2}]) === '[{"a":2,"b":1}]' (NOT '["[{\\"a\\":2,\\"b\\":1}]"]' — no double encoding)
    - canonicalJson on nested objects recursively sorts keys at every depth
    - hashablePayload strips _meta and schema_version keys before serializing
    - canonicalJson(["a","b"]) === '["a","b"]'
  </behavior>
  <action>
    Implement `packages/shared/src/canonical-json.ts` using the CORRECTED two-function form from RESEARCH.md Pattern 9: `sortedValue(payload: unknown): unknown` that returns a JS value tree (arrays → `payload.map(sortedValue)`, objects → `Object.fromEntries(Object.keys(obj).sort().map(k => [k, sortedValue(obj[k])]))`, primitives returned as-is), then `canonicalJson(payload): string = JSON.stringify(sortedValue(payload))` calling JSON.stringify exactly ONCE. Implement `hashablePayload(payload: Record<string,unknown>): string` destructuring out `_meta` and `schema_version` then calling canonicalJson on the rest. Do NOT use the broken `payload.map(canonicalJson)` form. Create `constants.ts` exporting `ZERO_HASH = '0'.repeat(64)`, `EVENT_TYPES = ['plan_created','task_spawned','memory_updated','conflict_detected','scope_closed'] as const`, `MAX_PARALLELISM = 4`, `MAX_CHILD_SCOPE_DEPTH = 3`, `MIN_CORPUS_THRESHOLD = 10`. Create `types.ts` exporting `EventLogNode`, `GraphWriteEvent`, `WriteResult` (with `occ_result: 'won' | 'demoted'`), `CanonicalEventType` (typeof EVENT_TYPES[number]). Create `schemas.ts` exporting `UUID_V4` regex, `HASH_HEX64 = /^[0-9a-f]{64}$/`, `CreateScopeSchema`, `EventBodySchema` (event_type enum of task_spawned/memory_updated/conflict_detected, entity_id UUID_V4, predecessor_hash HASH_HEX64, payload record) per RESEARCH.md Pattern 3. Create `index.ts` re-exporting all four modules. Write `src/__tests__/canonical-json.test.ts` covering all five behaviors above as vitest cases BEFORE confirming implementation.
  </action>
  <verify>
    <automated>cd D:/Repo/graph-enginerring && npm run test:unit -- canonical-json</automated>
  </verify>
  <acceptance_criteria>
    - `packages/shared/src/canonical-json.ts` contains `function sortedValue` and `Object.keys` and calls `JSON.stringify` exactly once inside `canonicalJson`
    - `packages/shared/src/canonical-json.ts` does NOT contain `payload.map(canonicalJson)`
    - `packages/shared/src/constants.ts` contains `ZERO_HASH` of 64 zeros and the 5 EVENT_TYPES
    - Test asserting `canonicalJson([{b:1,a:2}]) === '[{"a":2,"b":1}]'` passes
    - `npm run test:unit -- canonical-json` exits 0 with the canonical-json suite green
  </acceptance_criteria>
  <done>canonical-json suite green; shared package exports canonicalJson, hashablePayload, constants, types, Zod schemas.</done>
</task>

<task type="auto">
  <name>Task 3: PG integration test helper stub</name>
  <files>tests/helpers/pg-test-pool.ts</files>
  <read_first>
    - .planning/phases/01-core-graph-engine/01-RESEARCH.md (Standard Stack pg version, Environment Availability — DATABASE_URL, Docker pgvector image)
    - .planning/phases/01-core-graph-engine/01-VALIDATION.md (Wave 0 Requirements, integration test list)
  </read_first>
  <action>
    Create `tests/helpers/pg-test-pool.ts` exporting `getTestPool(): Pool` that constructs a `pg.Pool` from `process.env.DATABASE_URL` (default `postgres://postgres:postgres@localhost:5432/graph_test`), and `closeTestPool()`. Export `skipIfNoDb()` helper that returns true when `process.env.DATABASE_URL` is unset so integration suites can `describe.skipIf`. This is the shared fixture all Wave 2+ integration tests import. No schema creation here — migrations own DDL (Plan 02).
  </action>
  <verify>
    <automated>cd D:/Repo/graph-enginerring && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `tests/helpers/pg-test-pool.ts` contains `getTestPool` and reads `process.env.DATABASE_URL`
    - `tests/helpers/pg-test-pool.ts` exports `skipIfNoDb`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Integration test pool helper compiles and is importable by later integration suites.</done>
</task>

</tasks>

<verification>
- `npm install` succeeds; `npx tsc --noEmit` exits 0
- `npm run test:unit -- canonical-json` green
- `iii-config.yaml`, `tsconfig.json`, `vitest.config.ts` exist at repo root
</verification>

<success_criteria>
- Buildable monorepo with strict TypeScript and vitest
- canonical_json deterministic and array-encoding-correct (REQ-03)
- Shared package (canonicalJson, hashablePayload, constants, types, schemas) consumable by all later plans
</success_criteria>

<output>
Create `.planning/phases/01-core-graph-engine/01-01-SUMMARY.md` when done
</output>
