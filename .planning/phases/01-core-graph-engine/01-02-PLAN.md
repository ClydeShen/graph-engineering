---
phase: 01-core-graph-engine
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - migrations/001-extensions.sql
  - migrations/002-event-log.sql
  - migrations/003-memory-tables.sql
  - migrations/004-bus-state.sql
  - migrations/005-scope-lineage.sql
  - migrations/run-migrations.ts
  - tests/integration/schema.test.ts
autonomous: true
requirements: [REQ-01, REQ-04, REQ-05]
must_haves:
  truths:
    - "Running migrations creates execution_event_log partitioned BY LIST(scope_id)"
    - "execution_event_log payload column is TEXT (never JSONB) per ADR 02"
    - "Four memory tables exist with ts_doc tsvector GENERATED ALWAYS columns"
    - "procedural_memory has topology_embedding vector(128) with HNSW m=16 ef_construction=64"
    - "scope_lineage cold table exists and is not append-only"
  artifacts:
    - path: "migrations/002-event-log.sql"
      provides: "execution_event_log parent partitioned table"
      contains: "PARTITION BY LIST"
    - path: "migrations/003-memory-tables.sql"
      provides: "episodic/semantic/procedural/working memory tables"
      contains: "topology_embedding vector(128)"
    - path: "migrations/001-extensions.sql"
      provides: "pgcrypto + pgvector extensions"
      contains: "CREATE EXTENSION IF NOT EXISTS pgcrypto"
  key_links:
    - from: "migrations/002-event-log.sql"
      to: "pgcrypto"
      via: "extension dependency for digest()"
      pattern: "execution_event_log"
---

<objective>
Create PostgreSQL schema migrations: extensions (pgcrypto + pgvector), the partitioned `execution_event_log` parent table, four memory tables with tsvector + topology_embedding stubs, the `bus_state` HWM table, and the `scope_lineage` cold table. Provide a migration runner.

Purpose: This is the SSOT foundation. All later plans (OCC CTE, Control Plane, Queue Adapter) read/write these tables. The partition strategy, OCC constraint shape, and TEXT payload column are load-bearing ADR decisions.
Output: Idempotent ordered migrations + runner; schema integration test verifying structure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-graph-engine/01-CONTEXT.md
@.planning/phases/01-core-graph-engine/01-RESEARCH.md
@CONTEXT.md
@docs/ADR_v4.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extensions + execution_event_log partitioned parent (REQ-01)</name>
  <files>migrations/001-extensions.sql, migrations/002-event-log.sql</files>
  <read_first>
    - .planning/phases/01-core-graph-engine/01-RESEARCH.md (Pattern 4 OCC CTE column names, Open Question 2 payload column type TEXT, Scope Partition DDL example, Anti-pattern no jsonb)
    - .planning/phases/01-core-graph-engine/01-CONTEXT.md (PostgreSQL Schema decisions: PARTITION BY LIST, UNIQUE predecessor_hash scope_id, 5 event types)
    - docs/ADR_v4.md (ADR 01, ADR 02, ADR 11, ADR 12 schema spec)
  </read_first>
  <action>
    Create `migrations/001-extensions.sql` with `CREATE EXTENSION IF NOT EXISTS pgcrypto;` and `CREATE EXTENSION IF NOT EXISTS vector;`. Create `migrations/002-event-log.sql` defining parent table `execution_event_log` declared `PARTITION BY LIST (scope_id)`. Columns: `id BIGSERIAL`, `event_id BIGINT`, `scope_id UUID NOT NULL`, `entity_id UUID NOT NULL`, `event_type TEXT NOT NULL CHECK (event_type IN ('plan_created','task_spawned','memory_updated','conflict_detected','scope_closed'))` (enforces ADR 12 five-type rule at DB level), `predecessor_hash TEXT NOT NULL`, `version_hash TEXT NOT NULL`, `payload TEXT NOT NULL` (TEXT not JSONB — ADR 02 invariant; add a SQL comment explaining the no-::jsonb rule), `status TEXT NOT NULL DEFAULT 'pending_scheduling'`, `dispatched_at TIMESTAMPTZ`, `created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`. Primary key must include the partition key: `PRIMARY KEY (scope_id, id)`. Add a SQL comment documenting that the OCC `UNIQUE(predecessor_hash, scope_id)` and idempotency `UNIQUE(scope_id, entity_id, version_hash)` constraints are created per-partition at nesting time (Plan 04), NOT on the parent. Do NOT create partition sub-tables here.
  </action>
  <verify>
    <automated>cd D:/Repo/graph-enginerring && node -e "const s=require('fs').readFileSync('migrations/002-event-log.sql','utf8');process.exit(/PARTITION BY LIST/.test(s) && !/payload\s+JSONB/i.test(s) ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `migrations/001-extensions.sql` contains `CREATE EXTENSION IF NOT EXISTS pgcrypto` and `CREATE EXTENSION IF NOT EXISTS vector`
    - `migrations/002-event-log.sql` contains `PARTITION BY LIST (scope_id)` and `payload TEXT`
    - `migrations/002-event-log.sql` does NOT declare `payload JSONB`
    - `event_type` CHECK constraint lists exactly the 5 canonical types
  </acceptance_criteria>
  <done>Extensions and partitioned parent table migration written with TEXT payload and 5-type CHECK.</done>
</task>

<task type="auto">
  <name>Task 2: Memory tables + bus_state + scope_lineage (REQ-04, REQ-05)</name>
  <files>migrations/003-memory-tables.sql, migrations/004-bus-state.sql, migrations/005-scope-lineage.sql</files>
  <read_first>
    - .planning/phases/01-core-graph-engine/01-RESEARCH.md (Code Examples: pgvector HNSW Index DDL for procedural_memory; ts_doc GENERATED ALWAYS pattern)
    - .planning/phases/01-core-graph-engine/01-CONTEXT.md (Four memory tables decision: ts_doc tsvector GENERATED ALWAYS, topology_embedding vector(128) HNSW m=16 ef_construction=64; scope_lineage cold table)
    - docs/ADR_v4.md (ADR 20, ADR 23, ADR 25)
  </read_first>
  <action>
    Create `migrations/003-memory-tables.sql` defining four tables: `episodic_memory`, `semantic_memory`, `procedural_memory`, `working_memory`. Each gets a `content TEXT` column and `ts_doc TSVECTOR GENERATED ALWAYS AS (to_tsvector('english', coalesce(content,''))) STORED` plus a GIN index on `ts_doc`. `procedural_memory` additionally per RESEARCH.md Code Example: `intent_description TEXT`, `template_graph JSONB`, `quality_score FLOAT DEFAULT 0.5`, `is_anti_pattern BOOLEAN DEFAULT FALSE`, `topology_embedding vector(128)`, and an HNSW index `USING hnsw (topology_embedding vector_cosine_ops) WITH (m = 16, ef_construction = 64) WHERE is_anti_pattern = FALSE`. `semantic_memory` gets a `embedding vector(1536)` column (stub, no index Phase 1). All tables: `id UUID PRIMARY KEY DEFAULT gen_random_uuid()`, `scope_id UUID`, `created_at TIMESTAMPTZ DEFAULT NOW()`. Create `migrations/004-bus-state.sql`: `bus_state` table with `worker_id TEXT PRIMARY KEY`, `last_processed_event_id BIGINT NOT NULL DEFAULT 0` (HWM for Pulse-Fetch bridge). Create `migrations/005-scope-lineage.sql`: `scope_lineage` cold table (NOT append-only, NOT partitioned) with `scope_id UUID PRIMARY KEY`, `parent_scope_id UUID NULL`, `depth INT NOT NULL DEFAULT 0 CHECK (depth <= 3)`, `intent TEXT`, `status TEXT DEFAULT 'active'`, `created_at TIMESTAMPTZ DEFAULT NOW()`. Add comment marking it cold metadata written during DDL nesting protocol (ADR 23, ADR 05).
  </action>
  <verify>
    <automated>cd D:/Repo/graph-enginerring && node -e "const s=require('fs').readFileSync('migrations/003-memory-tables.sql','utf8');process.exit(/topology_embedding vector\(128\)/.test(s) && /m = 16, ef_construction = 64/.test(s) && /GENERATED ALWAYS/.test(s) ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `migrations/003-memory-tables.sql` defines all four tables: `episodic_memory`, `semantic_memory`, `procedural_memory`, `working_memory`
    - `migrations/003-memory-tables.sql` contains `topology_embedding vector(128)` and `m = 16, ef_construction = 64` and `GENERATED ALWAYS`
    - `migrations/004-bus-state.sql` contains `last_processed_event_id`
    - `migrations/005-scope-lineage.sql` contains `parent_scope_id` and `depth` with CHECK <= 3
  </acceptance_criteria>
  <done>Memory tables (with HNSW + tsvector), bus_state HWM, and scope_lineage cold table migrations written.</done>
</task>

<task type="auto">
  <name>Task 3: Migration runner + schema integration test</name>
  <files>migrations/run-migrations.ts, tests/integration/schema.test.ts</files>
  <read_first>
    - tests/helpers/pg-test-pool.ts (getTestPool, skipIfNoDb — created in Plan 01)
    - .planning/phases/01-core-graph-engine/01-VALIDATION.md (schema integration test row)
  </read_first>
  <action>
    Create `migrations/run-migrations.ts`: reads all `migrations/*.sql` files in lexical order, executes each against a `pg.Pool` from `DATABASE_URL`, logs each filename. Export `runMigrations(pool: Pool): Promise<void>` so tests can invoke it; also support `tsx migrations/run-migrations.ts` direct execution. Create `tests/integration/schema.test.ts` using `getTestPool` + `skipIfNoDb` from `tests/helpers/pg-test-pool.ts`: in beforeAll run migrations, then assert (a) `execution_event_log` exists and `relkind='p'` (partitioned) via `pg_class`, (b) `payload` column `data_type='text'` via `information_schema.columns`, (c) all four memory tables exist, (d) `procedural_memory` has an HNSW index via `pg_indexes` matching `hnsw`. Use `describe.skipIf(skipIfNoDb())` so the suite is skipped (not failed) when no DB.
  </action>
  <verify>
    <automated>cd D:/Repo/graph-enginerring && npm run test:integration -- schema</automated>
  </verify>
  <acceptance_criteria>
    - `migrations/run-migrations.ts` exports `runMigrations` and reads `migrations/*.sql` in order
    - `tests/integration/schema.test.ts` asserts `payload` data_type is `text`
    - `tests/integration/schema.test.ts` asserts `execution_event_log` relkind is `p`
    - `npm run test:integration -- schema` exits 0 (passes, or skips cleanly when DATABASE_URL unset)
  </acceptance_criteria>
  <done>Migration runner executes ordered SQL; schema test verifies partition + TEXT payload + HNSW index (or skips without DB).</done>
</task>

</tasks>

<verification>
- All migration SQL files lint-clean (parse without errors when DB available)
- Schema integration test passes or skips cleanly
- payload column is TEXT, parent is partitioned, HNSW index present on procedural_memory
</verification>

<success_criteria>
- execution_event_log PARTITION BY LIST(scope_id) with TEXT payload (REQ-01)
- Four memory tables with tsvector + topology_embedding HNSW (REQ-04)
- scope_lineage cold table (REQ-05)
</success_criteria>

<output>
Create `.planning/phases/01-core-graph-engine/01-02-SUMMARY.md` when done
</output>
