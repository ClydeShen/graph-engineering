---
phase: 01-core-graph-engine
plan: 06
type: execute
wave: 4
depends_on: ["01-03", "01-05"]
files_modified:
  - packages/workers/src/queue/queue-adapter.interface.ts
  - packages/workers/src/queue/pg-queue-adapter.ts
  - packages/workers/src/scheduler/frontier.worker.ts
  - packages/workers/src/scheduler/token-bucket.ts
  - src/__tests__/frontier.test.ts
  - tests/integration/queue.test.ts
autonomous: true
requirements: [REQ-17, REQ-18, REQ-19]
must_haves:
  truths:
    - "PgQueueAdapter implements IQueueAdapter and dequeues via FOR UPDATE SKIP LOCKED"
    - "Backpressure stops calling nextEvent() when all 4 worker slots occupied"
    - "Frontier Scheduler computes dynamic_score = base_priority*10 + age_bonus(<=20) + unlocks_count*5 with NO LLM call in dispatch path"
    - "Token bucket throttles frontier recompute to a 50ms window"
  artifacts:
    - path: "packages/workers/src/queue/pg-queue-adapter.ts"
      provides: "PgQueueAdapter with FOR UPDATE SKIP LOCKED + backpressure"
      contains: "FOR UPDATE SKIP LOCKED"
    - path: "packages/workers/src/scheduler/frontier.worker.ts"
      provides: "graph::scheduler::frontier Worker with priority SQL Top-K"
      contains: "dynamic_score"
  key_links:
    - from: "packages/workers/src/scheduler/frontier.worker.ts"
      to: "packages/workers/src/queue/pg-queue-adapter.ts"
      via: "dispatch claimed events"
      pattern: "nextEvent"
---

<objective>
Implement the PgQueueAdapter (IQueueAdapter interface, FOR UPDATE SKIP LOCKED, backpressure at Max_Parallelism=4, ON CONFLICT DO NOTHING idempotency) and the Frontier Scheduler Worker (graph::scheduler::frontier, 50ms token bucket, priority SQL Top-K — zero LLM calls in the dispatch path).

Purpose: REQ-17 (queue adapter), REQ-18 (idempotent claim), REQ-19 (frontier priority). The scheduler is pure SQL + Worker logic; iii is FIFO transport only.
Output: queue interface + adapter + frontier worker + token bucket; frontier unit test + queue integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-graph-engine/01-RESEARCH.md
@.planning/phases/01-core-graph-engine/01-CONTEXT.md
@docs/ADR_v4.md
@packages/shared/src/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: IQueueAdapter + PgQueueAdapter (REQ-17, REQ-18)</name>
  <files>packages/workers/src/queue/queue-adapter.interface.ts, packages/workers/src/queue/pg-queue-adapter.ts</files>
  <read_first>
    - .planning/phases/01-core-graph-engine/01-RESEARCH.md (Pattern 5 FOR UPDATE SKIP LOCKED, backpressure, dedicated Client for LISTEN; Anti-pattern Pool for LISTEN)
    - .planning/phases/01-core-graph-engine/01-CONTEXT.md (PgQueueAdapter FOR UPDATE SKIP LOCKED, IQueueAdapter abstraction, backpressure at 4 slots, idempotency UNIQUE)
    - .planning/REQUIREMENTS.md (REQ-17, REQ-18)
    - docs/adr/0034-adr32-pgqueueadapter-and-idempotency.md
  </read_first>
  <action>
    Create `queue-adapter.interface.ts` exporting `interface IQueueAdapter { nextEvent(scopeId: string): Promise<EventLogNode | null>; onWakeup(cb): void; }` (Phase 2 Redis replacement point — comment it). Create `pg-queue-adapter.ts` exporting `class PgQueueAdapter implements IQueueAdapter`. `nextEvent` runs the RESEARCH.md Pattern 5 SQL: `UPDATE execution_event_log SET status='processing', dispatched_at=NOW() WHERE id = (SELECT id FROM execution_event_log WHERE status='pending_dispatch' AND scope_id=$1 ORDER BY event_id ASC FOR UPDATE SKIP LOCKED LIMIT 1) RETURNING ...`. Use a dedicated non-pooled `new Client()` for LISTEN (`LISTEN graph_event_ready`) — NOT a Pool client (Anti-pattern). Implement backpressure: track `activeWorkerCount`; in the notification handler, `if (this.activeWorkerCount >= MAX_PARALLELISM) return;` (MAX_PARALLELISM=4 from constants). Document that all Worker writes go through the DO NOTHING idempotent path (REQ-18) — re-delivery is transparent.
  </action>
  <verify>
    <automated>cd D:/Repo/graph-enginerring && node -e "const s=require('fs').readFileSync('packages/workers/src/queue/pg-queue-adapter.ts','utf8');process.exit(/FOR UPDATE SKIP LOCKED/.test(s) && /MAX_PARALLELISM/.test(s) ? 0 : 1)" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `pg-queue-adapter.ts` contains `FOR UPDATE SKIP LOCKED` and `LIMIT 1`
    - `pg-queue-adapter.ts` uses a dedicated `new Client()` for LISTEN (not a Pool client)
    - backpressure guard references `MAX_PARALLELISM` and returns early when slots full
    - `queue-adapter.interface.ts` exports `IQueueAdapter`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>PgQueueAdapter dequeues with SKIP LOCKED, backpressures at 4 slots, behind IQueueAdapter.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Token bucket + Frontier Scheduler Worker (REQ-19)</name>
  <files>packages/workers/src/scheduler/token-bucket.ts, packages/workers/src/scheduler/frontier.worker.ts, src/__tests__/frontier.test.ts</files>
  <read_first>
    - .planning/phases/01-core-graph-engine/01-RESEARCH.md (Frontier Scheduler Priority SQL example, Pattern 1 registerTrigger durable subscriber topic)
    - .planning/phases/01-core-graph-engine/01-CONTEXT.md (Frontier Scheduler graph::scheduler::frontier, topic graph::frontier::changed, token bucket 50ms, priority formula, iii FIFO only)
    - .planning/REQUIREMENTS.md (REQ-19 exact formula)
    - docs/adr/0033-adr31-frontier-scheduler-architecture.md
  </read_first>
  <behavior>
    - dynamicScore(base=2, ageSec=10, unlocks=3) === 2*10 + min(10*10,20) + 3*5 === 20+20+15 === 55
    - age_bonus is capped at 20 regardless of age
    - ordering ties break by created_at ASC (FIFO)
    - computeFrontier performs NO LLM call (pure SQL + arithmetic)
  </behavior>
  <action>
    Create `token-bucket.ts` exporting `class TokenBucket` with a 50ms window: `tryAcquire(): boolean` returns false if called again within 50ms of the last grant (collapses cascade storms). Create `frontier.worker.ts` exporting the priority SQL from RESEARCH.md (`base_priority*10 + LEAST(age*10,20) + unlocks_count*5`, `ORDER BY dynamic_score DESC, created_at ASC LIMIT $2`) as `FRONTIER_PRIORITY_SQL`, and `class FrontierSchedulerWorker` registering function id `graph::scheduler::frontier` subscribed to topic `graph::frontier::changed` via `registerTrigger({ type: 'durable:subscriber', ... })`. Its handler: `if (!bucket.tryAcquire()) return;` then run the priority SQL with `LIMIT = MAX_PARALLELISM - activeCount`, dispatch Top-K via the queue adapter. Export pure `dynamicScore(base, ageSec, unlocks): number` for unit testing — NO LLM call anywhere in this file (add an assertion comment: dispatch path is LLM-free per ADR 31). Write `src/__tests__/frontier.test.ts` covering all four behaviors above.
  </action>
  <verify>
    <automated>cd D:/Repo/graph-enginerring && npm run test:unit -- frontier && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `frontier.worker.ts` exports `dynamicScore` and `FRONTIER_PRIORITY_SQL` containing `ORDER BY dynamic_score DESC, created_at ASC`
    - `frontier.test.ts` asserts `dynamicScore(2,10,3) === 55` and age_bonus capped at 20
    - `frontier.worker.ts` contains NO LLM/provider call (grep finds no chat/completions)
    - `token-bucket.ts` enforces a 50ms window
    - `npm run test:unit -- frontier` exits 0 and `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Frontier priority formula verified; token bucket throttles to 50ms; dispatch path is LLM-free.</done>
</task>

<task type="auto">
  <name>Task 3: PgQueueAdapter integration test (REQ-17, REQ-18)</name>
  <files>tests/integration/queue.test.ts</files>
  <read_first>
    - tests/helpers/pg-test-pool.ts (getTestPool, skipIfNoDb)
    - migrations/run-migrations.ts (runMigrations)
    - packages/workers/src/queue/pg-queue-adapter.ts (nextEvent)
    - .planning/phases/01-core-graph-engine/01-VALIDATION.md (queue test row)
  </read_first>
  <action>
    Create `tests/integration/queue.test.ts` with `describe.skipIf(skipIfNoDb())`: run migrations, create a test scope partition, insert N `pending_dispatch` events. Test (a) two parallel `nextEvent(scopeId)` calls via `Promise.all` return two DIFFERENT event ids (SKIP LOCKED prevents double-claim); (b) a claimed event has `status='processing'`; (c) when no pending events remain, `nextEvent` returns null. Document the idempotency assertion: re-inserting a claimed event with the same version_hash via DO NOTHING affects 0 rows.
  </action>
  <verify>
    <automated>cd D:/Repo/graph-enginerring && npm run test:integration -- queue</automated>
  </verify>
  <acceptance_criteria>
    - `queue.test.ts` asserts two parallel `nextEvent` calls return different ids
    - `queue.test.ts` asserts claimed event status is `processing`
    - `queue.test.ts` asserts `nextEvent` returns null when queue empty
    - `npm run test:integration -- queue` exits 0 (passes or skips cleanly without DB)
  </acceptance_criteria>
  <done>Queue integration test proves SKIP LOCKED no-double-claim and null-when-empty.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0; `npm run test:unit -- frontier` green; queue integration passes/skips
- Priority formula correct; LLM-free dispatch; SKIP LOCKED no double-claim
</verification>

<success_criteria>
- PgQueueAdapter FOR UPDATE SKIP LOCKED + backpressure + idempotency (REQ-17, REQ-18)
- Frontier Scheduler priority Top-K, 50ms token bucket, zero LLM in dispatch (REQ-19)
</success_criteria>

<output>
Create `.planning/phases/01-core-graph-engine/01-06-SUMMARY.md` when done
</output>
