---
phase: 01-core-graph-engine
plan: 10
type: execute
wave: 4
depends_on: ["01-05"]
files_modified:
  - packages/workers/src/patterns/discover.worker.ts
  - src/__tests__/pattern-discovery.test.ts
autonomous: true
requirements: [REQ-22]
must_haves:
  truths:
    - "graph::patterns::discover Worker registered with base_priority = 1 (lowest)"
    - "Cron runs every 6 hours via the iii-cron 7-field expression"
    - "Worker skips (returns early) when completed_scope_count < MIN_CORPUS_THRESHOLD (10)"
    - "Worker does NOT subscribe to scope_completed inline and does NOT consume the 4 OLTP worker slots"
  artifacts:
    - path: "packages/workers/src/patterns/discover.worker.ts"
      provides: "graph::patterns::discover cron Worker with MIN_CORPUS guard"
      contains: "MIN_CORPUS_THRESHOLD"
  key_links:
    - from: "packages/workers/src/patterns/discover.worker.ts"
      to: "packages/shared/src/constants.ts"
      via: "MIN_CORPUS_THRESHOLD import"
      pattern: "MIN_CORPUS_THRESHOLD"
---

<objective>
Implement the Pattern Discovery cron stub Worker: `graph::patterns::discover`, base_priority=1, cron every 6 hours, MIN_CORPUS=10 guard, no inline scope_completed subscription, no OLTP slot consumption.

Purpose: REQ-22 — Phase 1 ships the discovery scaffold with graceful cold-start degradation (single-session agent until corpus accumulates). Full pattern extraction is Phase 3.
Output: discover worker stub + cron registration; pattern-discovery unit test (guard).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-graph-engine/01-RESEARCH.md
@.planning/phases/01-core-graph-engine/01-CONTEXT.md
@docs/ADR_v4.md
@packages/shared/src/constants.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pattern Discovery cron stub Worker (REQ-22)</name>
  <files>packages/workers/src/patterns/discover.worker.ts, src/__tests__/pattern-discovery.test.ts</files>
  <read_first>
    - .planning/phases/01-core-graph-engine/01-RESEARCH.md (Pattern 1 registerTrigger cron 7-field; cron examples — every 6h = '0 0 */6 * * * *')
    - .planning/phases/01-core-graph-engine/01-CONTEXT.md (Pattern Discovery Stub: base_priority=1, cron 6h, MIN_CORPUS=10 guard, MUST NOT subscribe scope_completed inline, MUST NOT preempt OLTP slots, cold-start graceful degradation)
    - .planning/REQUIREMENTS.md (REQ-22)
    - packages/shared/src/constants.ts (MIN_CORPUS_THRESHOLD)
    - docs/adr/0039-adr37-pattern-discovery-schedule.md
  </read_first>
  <behavior>
    - runDiscovery returns early (skipped: true) when completed_scope_count < MIN_CORPUS_THRESHOLD (10)
    - runDiscovery proceeds (skipped: false) when completed_scope_count >= 10
    - the worker registers with base_priority = 1
    - the cron expression is the 7-field every-6-hours form '0 0 */6 * * * *'
  </behavior>
  <action>
    Create `discover.worker.ts` exporting `class PatternDiscoveryWorker` with `base_priority = 1` and `async runDiscovery(pool): Promise<{ skipped: boolean }>`. First query `SELECT count(*) FROM scope_lineage WHERE status = 'closed'` (or completed scope count); `if (count < MIN_CORPUS_THRESHOLD) return { skipped: true }` (cold-start graceful degradation — comment: system runs as single-session agent until corpus emerges). When count >= threshold, perform the Phase 1 stub body (comment: full WL-kernel pattern extraction is Phase 3) and return `{ skipped: false }`. Export a `registerPatternDiscovery(worker)` that registers function id `graph::patterns::discover` and a `registerTrigger({ type: 'cron', function_id: 'graph::patterns::discover', config: { expression: '0 0 */6 * * * *' } })`. Add a comment: MUST NOT subscribe to scope_completed inline and MUST NOT acquire OLTP worker slots (it runs only on cron, lowest priority). Write `src/__tests__/pattern-discovery.test.ts` (mock pool): assert skipped:true when count=9, skipped:false when count=10, and that base_priority===1.
  </action>
  <verify>
    <automated>cd D:/Repo/graph-enginerring && npm run test:unit -- pattern-discovery && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `discover.worker.ts` references `MIN_CORPUS_THRESHOLD` and returns early below it
    - `discover.worker.ts` sets `base_priority = 1`
    - cron registration uses `'0 0 */6 * * * *'` (7-field every-6-hours)
    - `discover.worker.ts` has a comment that it must not subscribe scope_completed inline nor consume OLTP slots
    - `pattern-discovery.test.ts` asserts skipped at count=9 and runs at count=10
    - `npm run test:unit -- pattern-discovery` exits 0 and `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Pattern Discovery cron stub with MIN_CORPUS guard, base_priority=1, 6h cron, no inline subscription, no OLTP slot use.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0; `npm run test:unit -- pattern-discovery` green
- MIN_CORPUS guard enforced; base_priority=1; 6h cron; no OLTP slot consumption
</verification>

<success_criteria>
- graph::patterns::discover cron Worker, base_priority=1, MIN_CORPUS=10 guard, cold-start degradation (REQ-22)
</success_criteria>

<output>
Create `.planning/phases/01-core-graph-engine/01-10-SUMMARY.md` when done
</output>
