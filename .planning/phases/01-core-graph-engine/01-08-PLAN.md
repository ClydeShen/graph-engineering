---
phase: 01-core-graph-engine
plan: 08
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - packages/shared/src/tokenizer.ts
  - packages/workers/src/context/knapsack.ts
  - packages/workers/src/context/overflow.ts
  - packages/workers/src/context/assemble.ts
  - src/__tests__/context-assembly.test.ts
autonomous: true
requirements: [REQ-10, REQ-20]
must_haves:
  truths:
    - "@dqbd/tiktoken get_encoding('cl100k_base') loaded once at module init; enc.free() on exit"
    - "3-layer prompt assembled: Layer 1 Stable, Layer 2 Context (Knapsack causal lineage), Layer 3 Volatile"
    - "Overflow is Zero-LLM reverse-chronological sliding window: newest events first, truncate at W_max, no LLM call, no context_compressed entity"
    - "IOverflowStrategy interface reserved but NOT activated in Phase 1"
  artifacts:
    - path: "packages/shared/src/tokenizer.ts"
      provides: "tiktoken Wasm singleton + countTokens"
      contains: "get_encoding"
    - path: "packages/workers/src/context/overflow.ts"
      provides: "ReverseChronologicalDiscarder Zero-LLM overflow"
      contains: "ReverseChronological"
    - path: "packages/workers/src/context/assemble.ts"
      provides: "3-layer prompt assembler"
      contains: "Stable"
  key_links:
    - from: "packages/workers/src/context/overflow.ts"
      to: "packages/shared/src/tokenizer.ts"
      via: "countTokens for W_max budget"
      pattern: "countTokens"
---

<objective>
Implement Context Assembly: the @dqbd/tiktoken Wasm tokenizer singleton (W_max budget), the Knapsack Slicing algorithm (causal-skeleton vertical axis + sibling horizontal axis), the 3-layer prompt builder (Stable / Context / Volatile), and the Zero-LLM reverse-chronological overflow discarder.

Purpose: REQ-10 (tokenizer) + REQ-20 (3-layer assembly + Zero-LLM overflow). Graph→Context is a one-way projection; overflow never mutates the graph and never calls an LLM.
Output: tokenizer + knapsack + overflow + assemble; context-assembly unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-graph-engine/01-RESEARCH.md
@.planning/phases/01-core-graph-engine/01-CONTEXT.md
@docs/ADR_v4.md
@CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: tiktoken Wasm tokenizer singleton (REQ-10)</name>
  <files>packages/shared/src/tokenizer.ts</files>
  <read_first>
    - .planning/phases/01-core-graph-engine/01-RESEARCH.md (Pattern 6 @dqbd/tiktoken exact load, Pitfall 4 Wasm memory management, Anti-pattern init in hot path)
    - .planning/phases/01-core-graph-engine/01-CONTEXT.md (Wasm tokenizer load example, W_max calculation only)
    - .planning/REQUIREMENTS.md (REQ-10)
  </read_first>
  <action>
    Create `packages/shared/src/tokenizer.ts` per RESEARCH.md Pattern 6: `import { get_encoding } from '@dqbd/tiktoken'`; call `const enc = get_encoding('cl100k_base')` ONCE at module top level (not in any function). Export `countTokens(text: string): number { return enc.encode(text).length }`. Register `process.on('exit', () => enc.free())` to release Wasm memory. Add a comment that this is used ONLY for W_max budget calculation (Pitfall 4 — never instantiate per request). Re-export from `packages/shared/src/index.ts`.
  </action>
  <verify>
    <automated>cd D:/Repo/graph-enginerring && node -e "const s=require('fs').readFileSync('packages/shared/src/tokenizer.ts','utf8');process.exit(/get_encoding\('cl100k_base'\)/.test(s) && /enc\.free\(\)/.test(s) ? 0 : 1)" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `tokenizer.ts` calls `get_encoding('cl100k_base')` exactly once at module level
    - `tokenizer.ts` registers `enc.free()` on process exit
    - `countTokens` does NOT call `get_encoding` (no per-call init)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Tokenizer singleton loads once, frees on exit, exposes countTokens.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Knapsack Slicing + Zero-LLM overflow discarder (REQ-20)</name>
  <files>packages/workers/src/context/knapsack.ts, packages/workers/src/context/overflow.ts, src/__tests__/context-assembly.test.ts</files>
  <read_first>
    - .planning/phases/01-core-graph-engine/01-CONTEXT.md (Knapsack Slicing vertical=predecessor_hash skeleton, horizontal=pending/conflict siblings; Overflow Zero-LLM reverse-chronological sliding window; IOverflowStrategy reserved not activated; Graph->Context one-way)
    - .planning/phases/01-core-graph-engine/01-RESEARCH.md (Frontier/Knapsack notes)
    - CONTEXT.md (Knapsack Slicing definition)
    - .planning/REQUIREMENTS.md (REQ-20)
    - docs/adr/0032-adr30-context-assembly-strategy.md
  </read_first>
  <behavior>
    - overflow discarder packs newest events first and stops when cumulative tokens would exceed W_max
    - overflow returns the newest-first subset; oldest events are dropped (not summarized)
    - overflow performs NO LLM call and creates NO context_compressed entity
    - knapsack walks predecessor_hash back to N_root (causal skeleton) before adding siblings
  </behavior>
  <action>
    Create `knapsack.ts` exporting `async function knapsackSlice(graph, scopeId, rootHash, wMax): Promise<EventLogNode[]>`: vertical axis walks `predecessor_hash` chain back to N_root (the causal skeleton), horizontal axis adds pending/conflict sibling events in the same scope, filling up to `wMax` tokens (via `countTokens`) in reverse-chronological order. Create `overflow.ts` exporting `interface IOverflowStrategy { discard(events, wMax): EventLogNode[] }` (RESERVED — add comment: NOT activated in Phase 1, ADR 30) and the active `class ReverseChronologicalDiscarder` implementing a Zero-LLM 3-tier lossy sliding window: sort events newest-first, accumulate `countTokens` until the next event would exceed `wMax`, physically truncate there. NO LLM call, NO `context_compressed` entity, NO graph mutation (assert via comment: Graph→Context one-way). Write `src/__tests__/context-assembly.test.ts` covering the four behaviors: newest-first packing, stop-at-budget, no LLM call (test passes no provider), oldest-dropped.
  </action>
  <verify>
    <automated>cd D:/Repo/graph-enginerring && npm run test:unit -- context-assembly && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `overflow.ts` exports `IOverflowStrategy` with a comment marking it reserved/not-activated
    - `overflow.ts` `ReverseChronologicalDiscarder` packs newest-first and truncates at `wMax`
    - `overflow.ts` contains NO call to any LLM provider and does NOT create `context_compressed`
    - `context-assembly.test.ts` asserts newest-first packing stops at budget and drops oldest
    - `npm run test:unit -- context-assembly` exits 0 and `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Knapsack causal-skeleton slicing + Zero-LLM reverse-chronological overflow with reserved (inactive) IOverflowStrategy.</done>
</task>

<task type="auto">
  <name>Task 3: 3-layer prompt assembler (REQ-20)</name>
  <files>packages/workers/src/context/assemble.ts</files>
  <read_first>
    - .planning/phases/01-core-graph-engine/01-CONTEXT.md (3-layer prompt: Layer 1 Stable cache-eligible, Layer 2 Context Knapsack projection, Layer 3 Volatile rebuilt each call)
    - packages/workers/src/context/knapsack.ts (knapsackSlice)
    - packages/workers/src/context/overflow.ts (ReverseChronologicalDiscarder)
    - packages/shared/src/tokenizer.ts (countTokens)
  </read_first>
  <action>
    Create `assemble.ts` exporting `async function assembleContext(graph, scopeId, rootHash, currentInput, wMax): Promise<{ stable, context, volatile }>`. Layer 1 Stable = the system role string (Anthropic prompt-cache eligible — keep stable across calls, add comment). Layer 2 Context = `knapsackSlice(...)`, and if the result exceeds `wMax` apply `ReverseChronologicalDiscarder.discard`. Layer 3 Volatile = `currentInput` serialized, rebuilt every call. Reserve `wMax` budget so `countTokens(stable) + countTokens(context) + countTokens(volatile) <= wMax`. On `scope_closed`, return context=null (signals Agent terminate, per ADR 24 — Gateway consumes this in Plan 07).
  </action>
  <verify>
    <automated>cd D:/Repo/graph-enginerring && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `assemble.ts` returns `{ stable, context, volatile }` (3 layers)
    - Layer 2 applies `ReverseChronologicalDiscarder` when over `wMax`
    - `assemble.ts` returns context=null on scope_closed
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>3-layer prompt assembler combining Stable + Knapsack context + Volatile within W_max.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0; `npm run test:unit -- context-assembly` green
- Overflow is Zero-LLM newest-first; IOverflowStrategy reserved not activated
- tiktoken loaded once
</verification>

<success_criteria>
- tiktoken Wasm singleton for W_max (REQ-10)
- 3-layer assembly + Zero-LLM reverse-chronological overflow (REQ-20)
</success_criteria>

<output>
Create `.planning/phases/01-core-graph-engine/01-08-SUMMARY.md` when done
</output>
