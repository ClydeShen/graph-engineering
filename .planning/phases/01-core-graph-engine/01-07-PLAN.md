---
phase: 01-core-graph-engine
plan: 07
type: execute
wave: 4
depends_on: ["01-03", "01-04", "01-05", "01-08"]
files_modified:
  - packages/gateway/package.json
  - packages/gateway/src/middleware/zod-guard.ts
  - packages/gateway/src/watchdog-sql.ts
  - packages/gateway/src/routes/scopes.ts
  - packages/gateway/src/routes/events.ts
  - packages/gateway/src/routes/scope-read.ts
  - packages/gateway/src/index.ts
  - src/__tests__/gateway.test.ts
autonomous: true
requirements: [REQ-15, REQ-16]
must_haves:
  truths:
    - "Three endpoints exist: POST /v1/scopes, POST /v1/scopes/:id/events, GET /v1/scopes/:id"
    - "Invalid UUID or hash returns 400 via Zod BEFORE any DB access"
    - "POST events runs OCC write, inline Watchdog SQL, and returns assembled Knapsack context"
    - "On scope_closed the events response returns context=null"
    - "Gateway holds direct-write rights for scope_closed and context_oom_throttled but NOT DDL"
  artifacts:
    - path: "packages/gateway/src/routes/events.ts"
      provides: "POST events: Zod -> OCC -> inline Watchdog -> context"
      contains: "occWrite"
    - path: "packages/gateway/src/middleware/zod-guard.ts"
      provides: "Zod validation 400 before DB"
      contains: "zValidator"
  key_links:
    - from: "packages/gateway/src/routes/events.ts"
      to: "packages/shared/src/occ-write.ts"
      via: "occWrite call"
      pattern: "occWrite"
    - from: "packages/gateway/src/routes/scopes.ts"
      to: "packages/control-plane/src/nesting.ts"
      via: "nestScope on scope create"
      pattern: "nestScope"
---

<objective>
Build the Hono HTTP Gateway: three REST endpoints with Zod validation (400 before any DB touch), the OCC write path on event submission, inline Watchdog SQL, direct-write rights for infrastructure events (scope_closed, context_oom_throttled), and synchronous Knapsack context assembly in the events response.

Purpose: REQ-15 (3 endpoints + non-stateless gateway) + REQ-16 (Zod validation). The Gateway is the External Participant entry point (ADR 24, ADR 29).
Output: gateway package + routes + zod-guard + watchdog-sql; gateway unit test (400 paths).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-graph-engine/01-RESEARCH.md
@.planning/phases/01-core-graph-engine/01-CONTEXT.md
@docs/ADR_v4.md
@packages/shared/src/schemas.ts
@packages/shared/src/occ-write.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hono app + Zod guard middleware (REQ-16)</name>
  <files>packages/gateway/package.json, packages/gateway/src/middleware/zod-guard.ts, packages/gateway/src/index.ts, src/__tests__/gateway.test.ts</files>
  <read_first>
    - .planning/phases/01-core-graph-engine/01-RESEARCH.md (Pattern 3 Hono + Zod exact API, @hono/zod-validator confirmed 0.8.0, UUID_V4 + HASH_HEX64 regexes)
    - .planning/phases/01-core-graph-engine/01-CONTEXT.md (Zod validation UUID v4 + hash regex, 400 immediately, Gateway not stateless)
    - packages/shared/src/schemas.ts (CreateScopeSchema, EventBodySchema, UUID_V4, HASH_HEX64)
    - .planning/REQUIREMENTS.md (REQ-16)
  </read_first>
  <behavior>
    - POST /v1/scopes/:id/events with non-UUID :id returns 400 and never queries the DB
    - POST /v1/scopes/:id/events with predecessor_hash not matching /^[0-9a-f]{64}$/ returns 400
    - POST /v1/scopes with empty intent returns 400
    - a valid request passes the guard (reaches handler)
  </behavior>
  <action>
    Create `packages/gateway/package.json` (name `@graph/gateway`, deps `hono@4.12.23`, `@hono/zod-validator@0.8.0`, `zod`, `pg`, `@graph/shared`, `@graph/control-plane`, `@graph/workers`). Create `zod-guard.ts` re-exporting `zValidator` usage helpers from `@hono/zod-validator` and a `validateScopeIdParam(id)` that tests `UUID_V4` and throws a 400 response BEFORE any DB call. Create `index.ts` building the Hono app, mounting the three route modules (Plan tasks 2-3), and `export default { port: Number(process.env.PORT ?? 3000), fetch: app.fetch }`. Inject a pg `Pool` (SELECT/INSERT only — no DDL). Write `src/__tests__/gateway.test.ts` using `app.request(...)` (Hono test client, no DB needed for 400 paths): assert all four behaviors above, and that a DB-spy pool is NOT called on the 400 paths.
  </action>
  <verify>
    <automated>cd D:/Repo/graph-enginerring && npm run test:unit -- gateway && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` deps include `hono`, `@hono/zod-validator`, and `@graph/workers`
    - `zod-guard.ts` validates scope id against `UUID_V4` and returns 400 before DB
    - `gateway.test.ts` asserts 400 on non-UUID :id with no DB call
    - `gateway.test.ts` asserts 400 on bad predecessor_hash
    - `npm run test:unit -- gateway` exits 0 and `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Hono app boots with Zod guard returning 400 before any DB access.</done>
</task>

<task type="auto">
  <name>Task 2: POST /v1/scopes + GET /v1/scopes/:id (REQ-15)</name>
  <files>packages/gateway/src/routes/scopes.ts, packages/gateway/src/routes/scope-read.ts</files>
  <read_first>
    - .planning/phases/01-core-graph-engine/01-RESEARCH.md (Pattern 3 route shapes)
    - .planning/phases/01-core-graph-engine/01-CONTEXT.md (POST /v1/scopes triggers DDL nesting; GET reads scope state)
    - packages/control-plane/src/nesting.ts (nestScope — created in Plan 04)
    - packages/shared/src/schemas.ts (CreateScopeSchema)
    - docs/adr/0026-adr24-agent-entry-point-protocol.md
  </read_first>
  <action>
    Create `scopes.ts`: `POST /v1/scopes` validated by `CreateScopeSchema`, calling `nestScope(intent)` from `@graph/control-plane` (the 3-phase DDL nesting protocol — Gateway does NOT do DDL itself; it delegates to Control Plane's nesting which owns the DDL pool). Return `201` with `{ scope_id, plan_hash, context }`. Create `scope-read.ts`: `GET /v1/scopes/:id` — validate UUID, read scope status from `scope_lineage` + latest event from `execution_event_log`, return `{ scope_id, status, context }`. Both use the SELECT/INSERT pool (no DDL).
  </action>
  <verify>
    <automated>cd D:/Repo/graph-enginerring && node -e "const s=require('fs').readFileSync('packages/gateway/src/routes/scopes.ts','utf8');process.exit(/nestScope/.test(s) ? 0 : 1)" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `scopes.ts` POST /v1/scopes calls `nestScope` and returns 201 with `scope_id`
    - `scope-read.ts` GET /v1/scopes/:id validates UUID and returns `status`
    - neither route issues DDL statements
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Scope create (delegates DDL nesting) and scope read endpoints implemented.</done>
</task>

<task type="auto">
  <name>Task 3: POST events + inline Watchdog + context (REQ-15)</name>
  <files>packages/gateway/src/routes/events.ts, packages/gateway/src/watchdog-sql.ts</files>
  <read_first>
    - .planning/phases/01-core-graph-engine/01-RESEARCH.md (Convergence Watchdog SQL example, Pattern 4 occWrite call site)
    - .planning/phases/01-core-graph-engine/01-CONTEXT.md (every POST events synchronously assembles Knapsack context; on scope_closed context=null; Gateway direct-write rights for scope_closed and context_oom_throttled, NOT DDL)
    - packages/shared/src/occ-write.ts (occWrite)
    - packages/workers/src/context/assemble.ts (assembleContext — created in Plan 08)
    - docs/adr/0026-adr24-agent-entry-point-protocol.md
  </read_first>
  <action>
    Create `watchdog-sql.ts` exporting the inline convergence SQL (RESEARCH.md Convergence Watchdog example) as `INLINE_WATCHDOG_SQL` and a helper `async function checkConvergence(pool, scopeId): Promise<{ isConverged, noOpenConflicts }>`. Create `events.ts`: `POST /v1/scopes/:id/events` validated by `EventBodySchema`. Flow: validate scope UUID (400 before DB) → `occWrite(pool, { scopeId, entityId, predecessorHash, payload })` → run `checkConvergence`; if converged AND no open conflicts, direct-write a `scope_closed` event (Gateway has this infra-write right per ADR 24) → assemble Knapsack context via `assembleContext`; if the scope is now closed return `{ version_hash, occ_result, context: null }` (signals Agent terminate), else return `{ version_hash, occ_result, context }`. Gateway may also direct-write `context_oom_throttled` when assembly reports OOM. Add a comment: Gateway holds infra-write rights for scope_closed + context_oom_throttled ONLY — DDL stays with Control Plane.
  </action>
  <verify>
    <automated>cd D:/Repo/graph-enginerring && node -e "const s=require('fs').readFileSync('packages/gateway/src/routes/events.ts','utf8');process.exit(/occWrite/.test(s) && /scope_closed/.test(s) && /context/.test(s) ? 0 : 1)" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `events.ts` calls `occWrite` then `checkConvergence` then `assembleContext`
    - `events.ts` returns `context: null` when scope is closed
    - `events.ts` direct-writes `scope_closed` (infra-write right) and references `context_oom_throttled`
    - `events.ts` issues NO DDL statements
    - `watchdog-sql.ts` exports `INLINE_WATCHDOG_SQL`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Events endpoint runs OCC + inline Watchdog + Knapsack context, returns context=null on close.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0; `npm run test:unit -- gateway` green (400 paths, no DB)
- Three endpoints present; OCC + inline Watchdog + context wired; no DDL in Gateway
</verification>

<success_criteria>
- Three REST endpoints with Zod 400-before-DB (REQ-15, REQ-16)
- Non-stateless Gateway: inline Watchdog + infra direct-writes + Knapsack context in response (REQ-15)
</success_criteria>

<output>
Create `.planning/phases/01-core-graph-engine/01-07-SUMMARY.md` when done
</output>
